[
  {
    "schema_name": "public",
    "function_name": "rpc_add_template_item",
    "identity_arguments": "p_template_section_id bigint, p_category_id bigint, p_question_type_id bigint, p_selection_mode text, p_question_count integer, p_difficulty_min smallint, p_difficulty_max smallint, p_avoid_recent_exams integer, p_passage_group_id integer, p_passage_count integer, p_passage_type text",
    "arguments": "p_template_section_id bigint, p_category_id bigint, p_question_type_id bigint, p_selection_mode text, p_question_count integer DEFAULT NULL::integer, p_difficulty_min smallint DEFAULT NULL::smallint, p_difficulty_max smallint DEFAULT NULL::smallint, p_avoid_recent_exams integer DEFAULT 0, p_passage_group_id integer DEFAULT NULL::integer, p_passage_count integer DEFAULT NULL::integer, p_passage_type text DEFAULT NULL::text",
    "return_type": "bigint",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_add_template_item(p_template_section_id bigint, p_category_id bigint, p_question_type_id bigint, p_selection_mode text, p_question_count integer DEFAULT NULL::integer, p_difficulty_min smallint DEFAULT NULL::smallint, p_difficulty_max smallint DEFAULT NULL::smallint, p_avoid_recent_exams integer DEFAULT 0, p_passage_group_id integer DEFAULT NULL::integer, p_passage_count integer DEFAULT NULL::integer, p_passage_type text DEFAULT NULL::text)\n RETURNS bigint\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_item_id bigint;\r\n    v_next_order integer;\r\nBEGIN\r\n\r\n    PERFORM rpc_assert_min_role('teacher');\r\n\r\n    -------------------------------------------------\r\n    -- validate template section\r\n    -------------------------------------------------\r\n    IF NOT EXISTS (\r\n        SELECT 1\r\n        FROM exam_template_sections\r\n        WHERE id = p_template_section_id\r\n    ) THEN\r\n        RAISE EXCEPTION 'Template section not found';\r\n    END IF;\r\n\r\n    -------------------------------------------------\r\n    -- validate category\r\n    -------------------------------------------------\r\n    IF NOT EXISTS (\r\n        SELECT 1\r\n        FROM question_categories\r\n        WHERE id = p_category_id\r\n    ) THEN\r\n        RAISE EXCEPTION 'Category not found';\r\n    END IF;\r\n\r\n    -------------------------------------------------\r\n    -- validate question type\r\n    -------------------------------------------------\r\n    IF NOT EXISTS (\r\n        SELECT 1\r\n        FROM question_types\r\n        WHERE id = p_question_type_id\r\n    ) THEN\r\n        RAISE EXCEPTION 'Question type not found';\r\n    END IF;\r\n\r\n    -------------------------------------------------\r\n    -- validate selection mode\r\n    -------------------------------------------------\r\n    IF p_selection_mode NOT IN ('QUESTION','PASSAGE') THEN\r\n        RAISE EXCEPTION 'Invalid selection_mode';\r\n    END IF;\r\n\r\n    -------------------------------------------------\r\n    -- validate counts\r\n    -------------------------------------------------\r\n    IF p_selection_mode = 'QUESTION' THEN\r\n\r\n        IF p_question_count IS NULL THEN\r\n            RAISE EXCEPTION 'question_count required for QUESTION mode';\r\n        END IF;\r\n\r\n        IF p_passage_count IS NOT NULL THEN\r\n            RAISE EXCEPTION 'passage_count must be null in QUESTION mode';\r\n        END IF;\r\n\r\n    END IF;\r\n\r\n    IF p_selection_mode = 'PASSAGE' THEN\r\n\r\n        IF p_passage_count IS NULL THEN\r\n            RAISE EXCEPTION 'passage_count required for PASSAGE mode';\r\n        END IF;\r\n\r\n        IF p_question_count IS NOT NULL THEN\r\n            RAISE EXCEPTION 'question_count must be null in PASSAGE mode';\r\n        END IF;\r\n\r\n    END IF;\r\n\r\n    -------------------------------------------------\r\n    -- ✅ FIX: passage_type consistency\r\n    -------------------------------------------------\r\n    IF p_selection_mode = 'QUESTION' AND p_passage_type IS NOT NULL THEN\r\n        RAISE EXCEPTION 'passage_type must be null in QUESTION mode';\r\n    END IF;\r\n\r\n    IF p_selection_mode = 'PASSAGE' AND p_passage_type IS NULL THEN\r\n        RAISE EXCEPTION 'passage_type required in PASSAGE mode';\r\n    END IF;\r\n\r\n    -------------------------------------------------\r\n    -- difficulty validation\r\n    -------------------------------------------------\r\n    IF p_difficulty_min IS NOT NULL\r\n       AND p_difficulty_max IS NOT NULL\r\n       AND p_difficulty_min > p_difficulty_max THEN\r\n        RAISE EXCEPTION 'Invalid difficulty range';\r\n    END IF;\r\n\r\n    -------------------------------------------------\r\n    -- avoid_recent_exams validation\r\n    -------------------------------------------------\r\n    IF p_avoid_recent_exams < 0 THEN\r\n        RAISE EXCEPTION 'avoid_recent_exams must be >= 0';\r\n    END IF;\r\n\r\n    -------------------------------------------------\r\n-- calculate item_order\r\n-------------------------------------------------\r\nSELECT COALESCE(MAX(item_order), 0) + 1\r\nINTO v_next_order\r\nFROM exam_template_items\r\nWHERE template_section_id = p_template_section_id;\r\n\r\n    -------------------------------------------------\r\n    -- insert\r\n    -------------------------------------------------\r\n    INSERT INTO public.exam_template_items(\r\n    template_section_id,\r\n    category_id,\r\n    question_type_id,\r\n    selection_mode,\r\n    question_count,\r\n    difficulty_min,\r\n    difficulty_max,\r\n    avoid_recent_exams,\r\n    passage_group_id,\r\n    passage_count,\r\n    passage_type,\r\n    item_order\r\n)\r\n    VALUES (\r\n        p_template_section_id,\r\n        p_category_id,\r\n        p_question_type_id,\r\n        p_selection_mode,\r\n        p_question_count,\r\n        p_difficulty_min,\r\n        p_difficulty_max,\r\n        p_avoid_recent_exams,\r\n        p_passage_group_id,\r\n        p_passage_count,\r\n        p_passage_type,\r\n        v_next_order\r\n    )\r\n    RETURNING id INTO v_item_id;\r\n\r\n    RETURN v_item_id;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_add_template_section",
    "identity_arguments": "p_template_id bigint, p_section_id bigint, p_section_order integer",
    "arguments": "p_template_id bigint, p_section_id bigint, p_section_order integer",
    "return_type": "bigint",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_add_template_section(p_template_id bigint, p_section_id bigint, p_section_order integer)\n RETURNS bigint\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\nDECLARE\r\n    v_template_section_id bigint;\r\nBEGIN\r\n\t\r\n\tperform rpc_assert_min_role('teacher');\r\n\t\r\n    -- validate order\r\n    IF p_section_order IS NULL OR p_section_order <= 0 THEN\r\n        RAISE EXCEPTION 'section_order must be > 0';\r\n    END IF;\r\n\r\n    -- validate template exists and active\r\n    IF NOT EXISTS (\r\n        SELECT 1\r\n        FROM exam_templates\r\n        WHERE id = p_template_id\r\n        AND is_active = true\r\n    ) THEN\r\n        RAISE EXCEPTION 'Template not found or inactive';\r\n    END IF;\r\n\r\n    -- prevent editing template if active exam exists\r\n    IF EXISTS (\r\n        SELECT 1\r\n        FROM exams\r\n        WHERE exam_template_id = p_template_id\r\n        AND submitted_at IS NULL\r\n    ) THEN\r\n        RAISE EXCEPTION 'Template locked because active exams exist';\r\n    END IF;\r\n\r\n    -- validate section exists\r\n    IF NOT EXISTS (\r\n        SELECT 1\r\n        FROM sections\r\n        WHERE id = p_section_id\r\n    ) THEN\r\n        RAISE EXCEPTION 'Section not found';\r\n    END IF;\r\n\r\n    -- prevent duplicate section\r\n    IF EXISTS (\r\n        SELECT 1\r\n        FROM exam_template_sections\r\n        WHERE exam_template_id = p_template_id\r\n        AND section_id = p_section_id\r\n    ) THEN\r\n        RAISE EXCEPTION 'Section already exists in template';\r\n    END IF;\r\n\r\n    -- prevent duplicate order\r\n    IF EXISTS (\r\n        SELECT 1\r\n        FROM exam_template_sections\r\n        WHERE exam_template_id = p_template_id\r\n        AND section_order = p_section_order\r\n    ) THEN\r\n        RAISE EXCEPTION 'section_order already used';\r\n    END IF;\r\n\r\n    INSERT INTO exam_template_sections(\r\n        exam_template_id,\r\n        section_id,\r\n        section_order\r\n    )\r\n    VALUES (\r\n        p_template_id,\r\n        p_section_id,\r\n        p_section_order\r\n    )\r\n    RETURNING id INTO v_template_section_id;\r\n\r\n    RETURN v_template_section_id;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_admin_hard_delete_exam",
    "identity_arguments": "p_exam_id bigint",
    "arguments": "p_exam_id bigint",
    "return_type": "boolean",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_admin_hard_delete_exam(p_exam_id bigint)\n RETURNS boolean\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exam_id bigint;\r\n    v_role text;\r\n\r\nBEGIN\r\n\r\n    ------------------------------------------------\r\n    -- Authorization\r\n    ------------------------------------------------\r\n    SELECT role\r\n    INTO v_role\r\n    FROM profiles\r\n    WHERE id = auth.uid();\r\n\r\n    IF v_role <> 'admin' THEN\r\n        RAISE EXCEPTION 'Admin permission required';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- Lock exam\r\n    ------------------------------------------------\r\n    SELECT id\r\n    INTO v_exam_id\r\n    FROM exams\r\n    WHERE id = p_exam_id\r\n    FOR UPDATE;\r\n\r\n    IF v_exam_id IS NULL THEN\r\n        RAISE EXCEPTION 'Exam not found';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- Delete answers\r\n    ------------------------------------------------\r\n    DELETE FROM user_answers\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Delete option snapshots\r\n    ------------------------------------------------\r\n    DELETE FROM exam_question_options\r\n    WHERE exam_question_id IN (\r\n        SELECT id\r\n        FROM exam_questions\r\n        WHERE exam_id = v_exam_id\r\n    );\r\n\r\n    ------------------------------------------------\r\n    -- Delete questions\r\n    ------------------------------------------------\r\n    DELETE FROM exam_questions\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Delete passages\r\n    ------------------------------------------------\r\n    DELETE FROM exam_passages\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Hard delete exam\r\n    ------------------------------------------------\r\n    DELETE FROM exams\r\n    WHERE id = v_exam_id;\r\n\r\n    RETURN true;\r\n\r\nEND;\r\n\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_assert_min_role",
    "identity_arguments": "p_required_role text",
    "arguments": "p_required_role text",
    "return_type": "void",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_assert_min_role(p_required_role text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public', 'pg_catalog'\nAS $function$\r\ndeclare\r\n    v_user_id uuid;\r\n    v_user_role text;\r\nbegin\r\n    -- 1. Authentication\r\n    v_user_id := auth.uid();\r\n    if v_user_id is null then\r\n        raise exception using message = 'UNAUTHENTICATED';\r\n    end if;\r\n\r\n    -- 2. Load role\r\n    select p.role\r\n    into v_user_role\r\n    from public.profiles p\r\n    where p.id = v_user_id\r\n      and p.is_active = true\r\n    limit 1;\r\n\r\n    if v_user_role is null then\r\n        raise exception using message = 'USER_NOT_FOUND_OR_INACTIVE';\r\n    end if;\r\n\r\n    -- 3. Validate role input\r\n    if p_required_role not in ('student','teacher','admin') then\r\n        raise exception using message = 'INVALID_REQUIRED_ROLE';\r\n    end if;\r\n\r\n    -- 4. Authorization (hierarchy)\r\n    if\r\n        (v_user_role = 'student' and p_required_role in ('teacher','admin')) or\r\n        (v_user_role = 'teacher' and p_required_role = 'admin')\r\n    then\r\n        raise exception using message = 'FORBIDDEN';\r\n    end if;\r\n\r\nend;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_check_exam_time",
    "identity_arguments": "p_exam_id bigint",
    "arguments": "p_exam_id bigint",
    "return_type": "TABLE(status text, remaining_seconds integer, end_time timestamp with time zone)",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_check_exam_time(p_exam_id bigint)\n RETURNS TABLE(status text, remaining_seconds integer, end_time timestamp with time zone)\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exam record;\r\n    v_now timestamptz := now();\r\nBEGIN\r\n\r\n    ------------------------------------------------\r\n    -- 1️⃣ Lock exam row (prevent race condition)\r\n    ------------------------------------------------\r\n    SELECT *\r\n    INTO v_exam\r\n    FROM exams\r\n    WHERE id = p_exam_id\r\n    FOR UPDATE;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'Exam not found: %', p_exam_id;\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 2️⃣ Compute end_time\r\n    ------------------------------------------------\r\n    end_time := v_exam.created_at + (v_exam.duration_minutes * interval '1 minute');\r\n\r\n    ------------------------------------------------\r\n    -- 3️⃣ Auto submit if timeout (SELF-HEALING)\r\n    ------------------------------------------------\r\n    IF v_exam.submitted_at IS NULL AND v_now >= end_time THEN\r\n        \r\n        -- idempotent submit (assumes rpc_submit_exam handles double call safely)\r\n        PERFORM public.rpc_submit_exam(p_exam_id);\r\n\r\n        -- reload exam after submit\r\n        SELECT *\r\n        INTO v_exam\r\n        FROM exams\r\n        WHERE id = p_exam_id;\r\n\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 4️⃣ Compute remaining time\r\n    ------------------------------------------------\r\n    IF v_exam.submitted_at IS NOT NULL THEN\r\n        remaining_seconds := 0;\r\n    ELSE\r\n        remaining_seconds := GREATEST(\r\n            0,\r\n            FLOOR(EXTRACT(EPOCH FROM (end_time - v_now)))\r\n        )::integer;\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 5️⃣ Determine status\r\n    ------------------------------------------------\r\n    IF v_exam.submitted_at IS NOT NULL THEN\r\n        status := 'submitted';\r\n    ELSIF v_now >= end_time THEN\r\n        status := 'timeout';\r\n    ELSE\r\n        status := 'in_progress';\r\n    END IF;\r\n\r\n    RETURN NEXT;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_create_exam_template",
    "identity_arguments": "p_name text, p_grade_level integer, p_duration_minutes integer",
    "arguments": "p_name text, p_grade_level integer, p_duration_minutes integer",
    "return_type": "bigint",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_create_exam_template(p_name text, p_grade_level integer, p_duration_minutes integer)\n RETURNS bigint\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\ndeclare\r\n    v_template_id bigint;\r\n\r\nbegin\r\n\t\r\n\tperform rpc_assert_min_role('teacher');\r\n\r\n    -- validation: name\r\n    if p_name is null or length(trim(p_name)) = 0 then\r\n        raise exception 'Template name cannot be empty';\r\n    end if;\r\n\r\n    -- validation: grade_level\r\n    if p_grade_level < 6 or p_grade_level > 12 then\r\n        raise exception 'Grade level must be between 6 and 12';\r\n    end if;\r\n\r\n    -- validation: duration\r\n    if p_duration_minutes is null then\r\n        raise exception 'Duration is required';\r\n    end if;\r\n\r\n    if p_duration_minutes < 5 or p_duration_minutes > 300 then\r\n        raise exception 'Duration must be between 5 and 300 minutes';\r\n    end if;\r\n\r\n    -- insert template\r\n    insert into public.exam_templates (\r\n        name,\r\n        grade_level,\r\n        duration_minutes\r\n    )\r\n    values (\r\n        trim(p_name),\r\n        p_grade_level,\r\n        p_duration_minutes\r\n    )\r\n    returning id into v_template_id;\r\n\r\n    return v_template_id;\r\n\r\nend;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_create_passage_with_questions",
    "identity_arguments": "p_content text, p_audio_url text, p_questions jsonb",
    "arguments": "p_content text, p_audio_url text, p_questions jsonb",
    "return_type": "bigint",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_create_passage_with_questions(p_content text, p_audio_url text, p_questions jsonb)\n RETURNS bigint\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\nDECLARE\r\n    v_passage_id bigint;\r\n\r\n    v_question jsonb;\r\n    v_question_type_code text;\r\n    v_question_type_id bigint;\r\n\r\n    v_distinct_types text[];\r\n\r\n    v_question_id bigint;\r\n\r\n    v_option jsonb;\r\n    v_answer text;\r\n\r\n    v_correct_count int;\r\nBEGIN\r\n\r\n    -- =========================\r\n    -- 1. BASIC VALIDATION\r\n    -- =========================\r\n    IF p_questions IS NULL OR jsonb_array_length(p_questions) = 0 THEN\r\n        RAISE EXCEPTION 'Questions cannot be empty';\r\n    END IF;\r\n\r\n    -- =========================\r\n    -- 2. ENSURE SAME TYPE\r\n    -- =========================\r\n    SELECT array_agg(DISTINCT q->>'question_type_code')\r\n    INTO v_distinct_types\r\n    FROM jsonb_array_elements(p_questions) q;\r\n\r\n    IF array_length(v_distinct_types, 1) != 1 THEN\r\n        RAISE EXCEPTION 'All questions must have same question_type';\r\n    END IF;\r\n\r\n    v_question_type_code := v_distinct_types[1];\r\n\r\n    -- =========================\r\n    -- 3. TYPE VALIDATION\r\n    -- =========================\r\n    IF v_question_type_code NOT IN (\r\n        'passage_mcq', 'passage_text', 'audio_mcq', 'audio_text'\r\n    ) THEN\r\n        RAISE EXCEPTION 'Invalid question_type for passage';\r\n    END IF;\r\n\r\n    -- =========================\r\n    -- 4. AUDIO CONSISTENCY\r\n    -- =========================\r\n    IF v_question_type_code LIKE 'audio_%' AND p_audio_url IS NULL THEN\r\n        RAISE EXCEPTION 'audio_url required for audio passage';\r\n    END IF;\r\n\r\n    -- =========================\r\n    -- 5. RESOLVE TYPE ID\r\n    -- =========================\r\n    SELECT id INTO v_question_type_id\r\n    FROM question_types\r\n    WHERE code = v_question_type_code;\r\n\r\n    IF v_question_type_id IS NULL THEN\r\n        RAISE EXCEPTION 'Invalid question_type_code';\r\n    END IF;\r\n\r\n    -- =========================\r\n    -- 6. STRICT VALIDATION LOOP (NO INSERT YET)\r\n    -- =========================\r\n    FOR v_question IN SELECT * FROM jsonb_array_elements(p_questions)\r\n    LOOP\r\n\r\n        IF v_question->>'content' IS NULL THEN\r\n            RAISE EXCEPTION 'Question content missing';\r\n        END IF;\r\n\r\n        -- ===== MCQ =====\r\n        IF v_question_type_code IN ('passage_mcq', 'audio_mcq') THEN\r\n\r\n            IF jsonb_array_length(v_question->'options') < 2 THEN\r\n                RAISE EXCEPTION 'MCQ must have at least 2 options';\r\n            END IF;\r\n\r\n            SELECT COUNT(*)\r\n            INTO v_correct_count\r\n            FROM jsonb_array_elements(v_question->'options') opt\r\n            WHERE (opt->>'is_correct')::boolean = true;\r\n\r\n            IF v_correct_count != 1 THEN\r\n                RAISE EXCEPTION 'MCQ must have exactly 1 correct option';\r\n            END IF;\r\n\r\n        END IF;\r\n\r\n        -- ===== TEXT =====\r\n        IF v_question_type_code IN ('passage_text', 'audio_text') THEN\r\n\r\n            IF jsonb_array_length(v_question->'text_answers') = 0 THEN\r\n                RAISE EXCEPTION 'Text question must have at least 1 answer';\r\n            END IF;\r\n\r\n        END IF;\r\n\r\n    END LOOP;\r\n\r\n    -- =========================\r\n    -- 7. INSERT PASSAGE\r\n    -- =========================\r\n    INSERT INTO passages (content, audio_url, created_by)\r\n    VALUES (p_content, p_audio_url, auth.uid())\r\n    RETURNING id INTO v_passage_id;\r\n\r\n    -- =========================\r\n    -- 8. INSERT QUESTIONS + CHILDREN\r\n    -- =========================\r\n    FOR v_question IN SELECT * FROM jsonb_array_elements(p_questions)\r\n    LOOP\r\n\r\n        INSERT INTO questions (\r\n            passage_id,\r\n            question_type_id,\r\n            content,\r\n            explanation,\r\n            difficulty,\r\n            created_by\r\n        )\r\n        VALUES (\r\n            v_passage_id,\r\n            v_question_type_id,\r\n            v_question->>'content',\r\n            v_question->>'explanation',\r\n            (v_question->>'difficulty')::smallint,\r\n            auth.uid()\r\n        )\r\n        RETURNING id INTO v_question_id;\r\n\r\n        -- ===== OPTIONS =====\r\n        IF v_question_type_code IN ('passage_mcq', 'audio_mcq') THEN\r\n            FOR v_option IN SELECT * FROM jsonb_array_elements(v_question->'options')\r\n            LOOP\r\n                INSERT INTO options (\r\n                    question_id,\r\n                    content,\r\n                    is_correct\r\n                )\r\n                VALUES (\r\n                    v_question_id,\r\n                    v_option->>'content',\r\n                    (v_option->>'is_correct')::boolean\r\n                );\r\n            END LOOP;\r\n        END IF;\r\n\r\n        -- ===== TEXT ANSWERS =====\r\n        IF v_question_type_code IN ('passage_text', 'audio_text') THEN\r\n            FOR v_answer IN\r\n                SELECT value FROM jsonb_array_elements_text(v_question->'text_answers')\r\n            LOOP\r\n                INSERT INTO question_text_answers (\r\n                    question_id,\r\n                    answer_text\r\n                )\r\n                VALUES (\r\n                    v_question_id,\r\n                    v_answer\r\n                );\r\n            END LOOP;\r\n        END IF;\r\n\r\n        -- ===== TAGS =====\r\n        IF v_question ? 'tag_ids' THEN\r\n            INSERT INTO question_tags (question_id, tag_id)\r\n            SELECT v_question_id, value::bigint\r\n            FROM jsonb_array_elements_text(v_question->'tag_ids');\r\n        END IF;\r\n\r\n    END LOOP;\r\n\r\n    RETURN v_passage_id;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_create_question",
    "identity_arguments": "p_question_type_code text, p_passage_id bigint, p_payload jsonb, p_tag_ids bigint[]",
    "arguments": "p_question_type_code text, p_passage_id bigint, p_payload jsonb, p_tag_ids bigint[]",
    "return_type": "bigint",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_create_question(p_question_type_code text, p_passage_id bigint, p_payload jsonb, p_tag_ids bigint[])\n RETURNS bigint\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n\r\n    v_question_type_id bigint;\r\n    v_question_id bigint;\r\n\r\n    v_question_text text;\r\n    v_explanation text;\r\n    v_grade_level integer;\r\n    v_difficulty smallint;\r\n    v_category_id bigint;\r\n\r\n    v_option_count integer := 0;\r\n    v_correct_count integer := 0;\r\n\r\n    v_is_mcq boolean := false;\r\n\r\nBEGIN\r\n\r\n\tperform rpc_assert_min_role('teacher');\r\n\r\n------------------------------------------------\r\n-- 1️⃣ Resolve question type\r\n------------------------------------------------\r\n\r\nSELECT id\r\nINTO v_question_type_id\r\nFROM question_types\r\nWHERE code = p_question_type_code;\r\n\r\nIF v_question_type_id IS NULL THEN\r\n    RAISE EXCEPTION 'Invalid question_type_code: %', p_question_type_code;\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- 2️⃣ Determine if MCQ\r\n------------------------------------------------\r\n\r\nIF p_question_type_code LIKE 'MCQ_%'\r\n   OR p_question_type_code LIKE 'PASSAGE_MCQ%'\r\n   OR p_question_type_code IN ('A1','A2','B2','B3','B4','B5','B6','D1','F1')\r\nTHEN\r\n    v_is_mcq := true;\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- 3️⃣ Validate passage rule\r\n------------------------------------------------\r\n\r\nIF p_question_type_code LIKE 'PASSAGE_%' AND p_passage_id IS NULL THEN\r\n    RAISE EXCEPTION 'Passage question requires passage_id';\r\nEND IF;\r\n\r\nIF p_question_type_code NOT LIKE 'PASSAGE_%' AND p_passage_id IS NOT NULL THEN\r\n    RAISE EXCEPTION 'Standalone question must not include passage_id';\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- 4️⃣ Lock passage + enforce single question_type\r\n------------------------------------------------\r\n\r\nIF p_passage_id IS NOT NULL THEN\r\n\r\n    PERFORM 1\r\n    FROM passages\r\n    WHERE id = p_passage_id\r\n    FOR UPDATE;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'Passage not found: %', p_passage_id;\r\n    END IF;\r\n\r\n    IF EXISTS (\r\n        SELECT 1\r\n        FROM questions\r\n        WHERE passage_id = p_passage_id\r\n        AND question_type_id <> v_question_type_id\r\n        LIMIT 1\r\n    ) THEN\r\n        RAISE EXCEPTION 'Passage already contains different question type';\r\n    END IF;\r\n\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- 5️⃣ Extract payload\r\n------------------------------------------------\r\n\r\nv_question_text := p_payload->>'question_text';\r\nv_explanation   := p_payload->>'explanation';\r\nv_grade_level   := (p_payload->>'grade_level')::integer;\r\nv_difficulty    := (p_payload->>'difficulty')::smallint;\r\nv_category_id   := (p_payload->>'category_id')::bigint;\r\n\r\nIF v_question_text IS NULL THEN\r\n    RAISE EXCEPTION 'question_text is required';\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- 6️⃣ Validate category\r\n------------------------------------------------\r\n\r\nIF v_category_id IS NOT NULL THEN\r\n\r\n    PERFORM 1\r\n    FROM question_categories\r\n    WHERE id = v_category_id;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'Invalid category_id: %', v_category_id;\r\n    END IF;\r\n\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- 7️⃣ Validate options if MCQ\r\n------------------------------------------------\r\n\r\nIF v_is_mcq THEN\r\n\r\n    v_option_count :=\r\n        jsonb_array_length(\r\n            COALESCE(p_payload->'options','[]'::jsonb)\r\n        );\r\n\r\n    IF v_option_count <> 4 THEN\r\n        RAISE EXCEPTION 'MCQ must contain exactly 4 options';\r\n    END IF;\r\n\r\n    SELECT COUNT(*)\r\n    INTO v_correct_count\r\n    FROM jsonb_to_recordset(p_payload->'options')\r\n    AS o(label text, text text, is_correct boolean)\r\n    WHERE is_correct = true;\r\n\r\n    IF v_correct_count = 0 THEN\r\n        RAISE EXCEPTION 'At least one correct option required';\r\n    END IF;\r\n\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- 8️⃣ Insert question\r\n------------------------------------------------\r\n\r\nINSERT INTO questions (\r\n    question_type_id,\r\n    passage_id,\r\n    question_text,\r\n    grade_level,\r\n    explanation,\r\n    difficulty,\r\n    category_id,\r\n    created_by\r\n)\r\nVALUES (\r\n    v_question_type_id,\r\n    p_passage_id,\r\n    v_question_text,\r\n    v_grade_level,\r\n    v_explanation,\r\n    v_difficulty,\r\n    v_category_id,\r\n    auth.uid()\r\n)\r\nRETURNING id INTO v_question_id;\r\n\r\n\r\n------------------------------------------------\r\n-- 9️⃣ Insert options\r\n------------------------------------------------\r\n\r\nIF v_is_mcq THEN\r\n\r\n    INSERT INTO options (\r\n        question_id,\r\n        option_label,\r\n        option_text,\r\n        is_correct\r\n    )\r\n    SELECT\r\n        v_question_id,\r\n        o.label,\r\n        o.text,\r\n        o.is_correct\r\n    FROM jsonb_to_recordset(p_payload->'options')\r\n    AS o(label text, text text, is_correct boolean);\r\n\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- 🔟 Insert tags\r\n------------------------------------------------\r\n\r\nIF p_tag_ids IS NOT NULL THEN\r\n\r\n    INSERT INTO question_tags (question_id, tag_id)\r\n    SELECT\r\n        v_question_id,\r\n        t.id\r\n    FROM unnest(p_tag_ids) t(id)\r\n    JOIN tags tg\r\n        ON tg.id = t.id;\r\n\r\nEND IF;\r\n\r\n\r\n------------------------------------------------\r\n-- return id\r\n------------------------------------------------\r\n\r\nRETURN v_question_id;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_delete_student_exam",
    "identity_arguments": "p_exam_id bigint",
    "arguments": "p_exam_id bigint",
    "return_type": "TABLE(success boolean, deleted_exam_id bigint)",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_delete_student_exam(p_exam_id bigint)\n RETURNS TABLE(success boolean, deleted_exam_id bigint)\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exam_id bigint;\r\n    v_deleted_at timestamptz;\r\nBEGIN\r\n\r\n\tperform rpc_assert_min_role('admin');\r\n\r\n    -- Lock exam row\r\n    SELECT id, deleted_at\r\n    INTO v_exam_id, v_deleted_at\r\n    FROM public.exams\r\n    WHERE id = p_exam_id\r\n    FOR UPDATE;\r\n\r\n    IF v_exam_id IS NULL THEN\r\n        RAISE EXCEPTION 'Exam not found';\r\n    END IF;\r\n\r\n    IF v_deleted_at IS NOT NULL THEN\r\n        RAISE EXCEPTION 'Exam already deleted';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- Delete user answers\r\n    ------------------------------------------------\r\n    DELETE FROM public.user_answers\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Delete question options snapshot\r\n    ------------------------------------------------\r\n    DELETE FROM public.exam_question_options\r\n    WHERE exam_question_id IN (\r\n        SELECT id\r\n        FROM public.exam_questions\r\n        WHERE exam_id = v_exam_id\r\n    );\r\n\r\n    ------------------------------------------------\r\n    -- Delete exam questions snapshot\r\n    ------------------------------------------------\r\n    DELETE FROM public.exam_questions\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Delete exam passages snapshot\r\n    ------------------------------------------------\r\n    DELETE FROM public.exam_passages\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Soft delete exam\r\n    ------------------------------------------------\r\n    UPDATE public.exams\r\n    SET\r\n        deleted_at = now(),\r\n        updated_at = now()\r\n    WHERE id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Return result\r\n    ------------------------------------------------\r\n    RETURN QUERY\r\n    SELECT true, v_exam_id;\r\n\r\nEND;\r\n\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_ensure_exam_active",
    "identity_arguments": "p_exam_id bigint",
    "arguments": "p_exam_id bigint",
    "return_type": "exams",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_ensure_exam_active(p_exam_id bigint)\n RETURNS exams\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exam exams%rowtype;\r\n    v_now timestamptz := now();\r\n    v_end_time timestamptz;\r\nBEGIN\r\n\r\n    ------------------------------------------------\r\n    -- 1️⃣ Lock exam row\r\n    ------------------------------------------------\r\n    SELECT *\r\n    INTO v_exam\r\n    FROM exams\r\n    WHERE id = p_exam_id\r\n      AND user_id = auth.uid()\r\n      AND is_active = true\r\n      AND deleted_at IS NULL\r\n    FOR UPDATE;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'Exam not found or access denied';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 2️⃣ Already submitted → return immediately\r\n    ------------------------------------------------\r\n    IF v_exam.submitted_at IS NOT NULL THEN\r\n        RETURN v_exam;\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 3️⃣ Compute end_time\r\n    ------------------------------------------------\r\n    v_end_time :=\r\n        v_exam.created_at\r\n        + (v_exam.duration_minutes * interval '1 minute');\r\n\r\n    ------------------------------------------------\r\n    -- 4️⃣ Auto submit if expired (SELF-HEALING)\r\n    ------------------------------------------------\r\n    IF v_now >= v_end_time THEN\r\n\r\n        PERFORM public.rpc_submit_exam(p_exam_id);\r\n\r\n        -- reload after submit\r\n        SELECT *\r\n        INTO v_exam\r\n        FROM exams\r\n        WHERE id = p_exam_id;\r\n\r\n        RETURN v_exam;\r\n\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 5️⃣ Still active → return\r\n    ------------------------------------------------\r\n    RETURN v_exam;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_get_available_exam_templates",
    "identity_arguments": "p_grade_level integer",
    "arguments": "p_grade_level integer",
    "return_type": "TABLE(id bigint, name text, duration_minutes integer, grade_level integer, sections integer, total_questions integer, difficulty text, status text, created_at timestamp with time zone)",
    "language": "sql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_get_available_exam_templates(p_grade_level integer)\n RETURNS TABLE(id bigint, name text, duration_minutes integer, grade_level integer, sections integer, total_questions integer, difficulty text, status text, created_at timestamp with time zone)\n LANGUAGE sql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\nwith section_stats as (\r\n    select\r\n        ts.exam_template_id,\r\n        count(distinct ts.id) as sections,\r\nsum(coalesce(ti.question_count, 0)) as total_questions,\r\n        min(ti.difficulty_min) as min_diff,\r\n        max(ti.difficulty_max) as max_diff\r\n    from exam_template_sections ts\r\n    join exam_template_items ti\r\n        on ti.template_section_id = ts.id\r\n    group by ts.exam_template_id\r\n)\r\nselect\r\n    et.id,\r\n    et.name,\r\n    et.duration_minutes,\r\n    et.grade_level,\r\n    coalesce(ss.sections,0) as sections,\r\n    coalesce(ss.total_questions,0) as total_questions,\r\n\r\n    case\r\n        when ss.min_diff is null then 'Unknown'\r\n        when ss.min_diff >= 1 and ss.max_diff <= 2 then 'Easy'\r\n        when ss.min_diff = 3 and ss.max_diff = 3 then 'Medium'\r\n        when ss.min_diff >= 4 then 'Hard'\r\n        else 'Mixed'\r\n    end as difficulty,\r\n\r\n    case\r\n        when et.is_active then 'Active'\r\n        else 'Inactive'\r\n    end as status,\r\n\r\n    et.created_at\r\n\r\nfrom exam_templates et\r\nleft join section_stats ss\r\n    on ss.exam_template_id = et.id\r\n\r\nwhere\r\n    et.grade_level = p_grade_level\r\n    and et.deleted_at is null\r\n\r\norder by\r\n    et.created_at desc,\r\n    et.id desc;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_get_exam_result",
    "identity_arguments": "p_exam_id bigint",
    "arguments": "p_exam_id bigint",
    "return_type": "jsonb",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_get_exam_result(p_exam_id bigint)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\ndeclare\r\n    v_user_id uuid;\r\n    v_total_questions int;\r\n    v_correct_answers int;\r\n    v_score numeric;\r\n    v_result jsonb;\r\nbegin\r\n\r\n\tperform public.rpc_assert_min_role('student');\r\n\r\n    v_user_id := auth.uid();\r\n\r\n    ----------------------------------------------------------------\r\n    -- verify exam ownership\r\n    ----------------------------------------------------------------\r\n    if not exists (\r\n        select 1\r\n        from exams\r\n        where id = p_exam_id\r\n        and user_id = v_user_id\r\n    ) then\r\n        raise exception 'exam not found';\r\n    end if;\r\n\r\n    ----------------------------------------------------------------\r\n    -- ensure exam is submitted (avoid answer leak)\r\n    ----------------------------------------------------------------\r\n    if not exists (\r\n        select 1\r\n        from exams\r\n        where id = p_exam_id\r\n        and submitted_at is not null\r\n    ) then\r\n        raise exception 'exam not submitted';\r\n    end if;\r\n\r\n    ----------------------------------------------------------------\r\n    -- total questions\r\n    ----------------------------------------------------------------\r\n    select count(*)\r\n    into v_total_questions\r\n    from exam_questions\r\n    where exam_id = p_exam_id;\r\n\r\n    ----------------------------------------------------------------\r\n    -- correct answers\r\n    ----------------------------------------------------------------\r\n    select count(*)\r\n    into v_correct_answers\r\n    from user_answers\r\n    where exam_id = p_exam_id\r\n    and user_id = v_user_id\r\n    and is_correct = true;\r\n\r\n    ----------------------------------------------------------------\r\n    -- score calculation\r\n    ----------------------------------------------------------------\r\n    if v_total_questions = 0 then\r\n        v_score := 0;\r\n    else\r\n        v_score := round((v_correct_answers::numeric / v_total_questions) * 100, 2);\r\n    end if;\r\n\r\n    ----------------------------------------------------------------\r\n    -- build result json\r\n    ----------------------------------------------------------------\r\n    select jsonb_build_object(\r\n        'exam_id', p_exam_id,\r\n        'score', v_score,\r\n        'total_questions', v_total_questions,\r\n        'correct_answers', v_correct_answers,\r\n        'questions',\r\n        coalesce(\r\n            jsonb_agg(\r\n                jsonb_build_object(\r\n                    'exam_question_id', q.id,\r\n                    'question_text', q.question_text,\r\n                    'explanation', q.explanation,\r\n                    'selected_option_label', ua.selected_option_label,\r\n                    'correct_option_label', q.correct_option_label,\r\n                    'is_correct', ua.is_correct,\r\n                    'options',\r\n                        (\r\n                            select coalesce(\r\n                                jsonb_agg(\r\n                                    jsonb_build_object(\r\n                                        'option_id', o.id,\r\n                                        'label', o.label,\r\n                                        'text', o.option_text,\r\n                                        'is_correct', o.is_correct\r\n                                    )\r\n                                    order by o.label\r\n                                ),\r\n                                '[]'::jsonb\r\n                            )\r\n                            from exam_question_options o\r\n                            where o.exam_question_id = q.id\r\n                        )\r\n                )\r\n                order by q.id\r\n            ),\r\n            '[]'::jsonb\r\n        )\r\n    )\r\n    into v_result\r\n    from exam_questions q\r\n    left join user_answers ua\r\n        on ua.exam_question_id = q.id\r\n        and ua.exam_id = p_exam_id\r\n        and ua.user_id = v_user_id\r\n    where q.exam_id = p_exam_id;\r\n\r\n    return v_result;\r\n\r\nend;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_get_exam_structure",
    "identity_arguments": "p_template_id bigint",
    "arguments": "p_template_id bigint",
    "return_type": "jsonb",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_get_exam_structure(p_template_id bigint)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\ndeclare\r\n    v_result jsonb;\r\nbegin\r\n\t\r\n\tperform public.rpc_assert_min_role('student');\r\n\r\n    -- Validate template\r\n    if not exists (\r\n        select 1\r\n        from exam_templates\r\n        where id = p_template_id\r\n        and is_active = true\r\n        and deleted_at is null\r\n    ) then\r\n        raise exception 'Active exam template % not found', p_template_id;\r\n    end if;\r\n\r\n\r\n    select jsonb_build_object(\r\n        'template_id', t.id,\r\n        'template_name', t.name,\r\n        'grade_level', t.grade_level,\r\n        'duration_minutes', t.duration_minutes,\r\n\r\n        'sections',\r\n        coalesce(\r\n\r\n            (\r\n                select jsonb_agg(\r\n\r\n                    jsonb_build_object(\r\n                        'section_id', s.id,\r\n                        'section_code', s.code,\r\n                        'section_name', s.name,\r\n                        'section_order', ts.section_order,\r\n\r\n                        'items',\r\n                        coalesce(\r\n\r\n                            (\r\n                                select jsonb_agg(\r\n\r\n                                    jsonb_build_object(\r\n                                        'item_id', ti.id,\r\n                                        'item_order', ti.item_order,\r\n                                        'category_id', ti.category_id,\r\n                                        'question_type_id', ti.question_type_id,\r\n                                        'question_count', ti.question_count,\r\n\r\n                                        'difficulty_min', ti.difficulty_min,\r\n                                        'difficulty_max', ti.difficulty_max,\r\n\r\n                                        'passage_type', ti.passage_type,\r\n                                        'passage_count', ti.passage_count,\r\n                                        'passage_group_id', ti.passage_group_id,\r\n\r\n                                        'avoid_recent_exams', ti.avoid_recent_exams\r\n                                    )\r\n                                    order by ti.item_order\r\n                                )\r\n                                from exam_template_items ti\r\n                                where ti.template_section_id = ts.id\r\n                            ),\r\n\r\n                            '[]'::jsonb\r\n\r\n                        )\r\n\r\n                    )\r\n\r\n                    order by ts.section_order\r\n\r\n                )\r\n                from exam_template_sections ts\r\n                join sections s\r\n                    on s.id = ts.section_id\r\n                where ts.exam_template_id = t.id\r\n            ),\r\n\r\n            '[]'::jsonb\r\n\r\n        )\r\n    )\r\n    into v_result\r\n    from exam_templates t\r\n    where t.id = p_template_id;\r\n\r\n    return v_result;\r\n\r\nend;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_get_exam_template_detail",
    "identity_arguments": "p_template_id bigint",
    "arguments": "p_template_id bigint",
    "return_type": "jsonb",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_get_exam_template_detail(p_template_id bigint)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\ndeclare\r\n  v_result jsonb;\r\nbegin\r\n  -- role check\r\n  perform public.rpc_assert_min_role('teacher');\r\n\r\n  -- validate\r\n  if not exists (\r\n    select 1 from public.exam_templates t\r\n    where t.id = p_template_id\r\n  ) then\r\n    raise exception 'Exam template not found: %', p_template_id;\r\n  end if;\r\n\r\n  select jsonb_build_object(\r\n    'id', t.id,\r\n    'name', t.name,\r\n    'description', t.description,\r\n    'sections', coalesce(sec.sections, '[]'::jsonb)\r\n  )\r\n  into v_result\r\n  from public.exam_templates t\r\n\r\n  left join lateral (\r\n    select jsonb_agg(\r\n      jsonb_build_object(\r\n        'id', s.id,\r\n        'section_id', s.section_id,\r\n        'section_order', s.section_order,\r\n        'items', coalesce(it.items, '[]'::jsonb)\r\n      )\r\n      order by s.section_order\r\n    ) as sections\r\n    from public.exam_template_sections s\r\n\r\n    left join lateral (\r\n      select jsonb_agg(\r\n        jsonb_build_object(\r\n          'id', i.id,\r\n          'question_count', i.question_count,\r\n          'passage_count', i.passage_count,\r\n          'selection_mode', i.selection_mode,\r\n          'difficulty_min', i.difficulty_min,\r\n          'difficulty_max', i.difficulty_max,\r\n          'passage_type', i.passage_type,\r\n          'category', jsonb_build_object(\r\n            'id', c.id,\r\n            'name', c.name\r\n          ),\r\n          'question_type', jsonb_build_object(\r\n            'id', qt.id,\r\n            'code', qt.code,\r\n            'name', qt.name\r\n          )\r\n        )\r\n        order by i.item_order\r\n      ) as items\r\n      from public.exam_template_items i\r\n      join public.question_categories c on c.id = i.category_id\r\n      join public.question_types qt on qt.id = i.question_type_id\r\n      where i.template_section_id = s.id\r\n    ) it on true\r\n\r\n    where s.exam_template_id = t.id\r\n  ) sec on true\r\n\r\n  where t.id = p_template_id;\r\n\r\n  return v_result;\r\nend;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_get_passage_detail",
    "identity_arguments": "p_passage_id bigint",
    "arguments": "p_passage_id bigint",
    "return_type": "jsonb",
    "language": "plpgsql",
    "provolatile": "s",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_get_passage_detail(p_passage_id bigint)\n RETURNS jsonb\n LANGUAGE plpgsql\n STABLE SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\nDECLARE\r\n    v_result jsonb;\r\nBEGIN\r\n\r\n    SELECT jsonb_build_object(\r\n        'id', p.id,\r\n        'content', p.content,\r\n        'audio_url', p.audio_url,\r\n\r\n        'questions',\r\n        COALESCE((\r\n            SELECT jsonb_agg(\r\n                jsonb_build_object(\r\n                    'id', q.id,\r\n                    'question_type_code', qt.code,\r\n                    'content', q.content,\r\n                    'explanation', q.explanation,\r\n                    'difficulty', q.difficulty,\r\n\r\n                    -- options\r\n                    'options', COALESCE((\r\n                        SELECT jsonb_agg(\r\n                            jsonb_build_object(\r\n                                'content', o.content,\r\n                                'is_correct', o.is_correct\r\n                            )\r\n                            ORDER BY o.id\r\n                        )\r\n                        FROM options o\r\n                        WHERE o.question_id = q.id\r\n                    ), '[]'::jsonb),\r\n\r\n                    -- text answers\r\n                    'text_answers', COALESCE((\r\n                        SELECT jsonb_agg(a.answer_text ORDER BY a.id)\r\n                        FROM question_text_answers a\r\n                        WHERE a.question_id = q.id\r\n                    ), '[]'::jsonb),\r\n\r\n                    -- tags\r\n                    'tag_ids', COALESCE((\r\n                        SELECT jsonb_agg(t.tag_id)\r\n                        FROM question_tags t\r\n                        WHERE t.question_id = q.id\r\n                    ), '[]'::jsonb)\r\n\r\n                )\r\n                ORDER BY q.id\r\n            )\r\n            FROM questions q\r\n            JOIN question_types qt ON qt.id = q.question_type_id\r\n            WHERE q.passage_id = p.id\r\n              AND q.deleted_at IS NULL\r\n        ), '[]'::jsonb)\r\n    )\r\n    INTO v_result\r\n    FROM passages p\r\n    WHERE p.id = p_passage_id;\r\n\r\n    IF v_result IS NULL THEN\r\n        RAISE EXCEPTION 'PASSAGE_NOT_FOUND';\r\n    END IF;\r\n\r\n    RETURN v_result;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_get_question_detail",
    "identity_arguments": "p_question_id bigint",
    "arguments": "p_question_id bigint",
    "return_type": "jsonb",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_get_question_detail(p_question_id bigint)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_result jsonb;\r\nBEGIN\r\n    -- 1. Auth guard\r\n    PERFORM public.rpc_assert_min_role('teacher');\r\n\r\n    -- 2. Main query\r\n    SELECT jsonb_build_object(\r\n        'id', q.id,\r\n        'question_text', q.question_text,\r\n        'explanation', q.explanation,\r\n        'difficulty', q.difficulty,\r\n        'question_type', qt.code,\r\n\r\n        -- Passage (nullable)\r\n        'passage', CASE\r\n            WHEN p.id IS NULL THEN NULL\r\n            ELSE jsonb_build_object(\r\n                'id', p.id,\r\n                'content', p.content,\r\n                'audio_url', p.audio_url\r\n            )\r\n        END,\r\n\r\n        -- Options\r\n        'options', COALESCE((\r\n            SELECT jsonb_agg(\r\n                jsonb_build_object(\r\n                    'id', o.id,\r\n                    'content', o.content,\r\n                    'is_correct', o.is_correct,\r\n                    'position', o.position\r\n                )\r\n                ORDER BY o.position\r\n            )\r\n            FROM options o\r\n            WHERE o.question_id = q.id\r\n        ), '[]'::jsonb),\r\n\r\n        -- Tags\r\n        'tags', COALESCE((\r\n            SELECT jsonb_agg(\r\n                jsonb_build_object(\r\n                    'id', t.id,\r\n                    'name', t.name\r\n                )\r\n            )\r\n            FROM question_tags qt2\r\n            JOIN tags t ON t.id = qt2.tag_id\r\n            WHERE qt2.question_id = q.id\r\n        ), '[]'::jsonb)\r\n\r\n    )\r\n    INTO v_result\r\n    FROM questions q\r\n    JOIN question_types qt ON qt.id = q.question_type_id\r\n    LEFT JOIN passages p ON p.id = q.passage_id\r\n    WHERE q.id = p_question_id\r\n      AND q.deleted_at IS NULL;\r\n\r\n    -- 3. Not found guard (includes soft delete)\r\n    IF v_result IS NULL THEN\r\n        RAISE EXCEPTION 'Question not found';\r\n    END IF;\r\n\r\n    RETURN v_result;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_get_student_exams",
    "identity_arguments": "p_user_id uuid, p_limit integer, p_offset integer",
    "arguments": "p_user_id uuid, p_limit integer DEFAULT 20, p_offset integer DEFAULT 0",
    "return_type": "TABLE(exam_id bigint, exam_template_id bigint, grade_level integer, score integer, total_questions integer, accuracy numeric, exp_earned integer, created_at timestamp with time zone, submitted_at timestamp with time zone, duration_seconds integer, exam_status text, is_active boolean, total_count bigint)",
    "language": "sql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_get_student_exams(p_user_id uuid, p_limit integer DEFAULT 20, p_offset integer DEFAULT 0)\n RETURNS TABLE(exam_id bigint, exam_template_id bigint, grade_level integer, score integer, total_questions integer, accuracy numeric, exp_earned integer, created_at timestamp with time zone, submitted_at timestamp with time zone, duration_seconds integer, exam_status text, is_active boolean, total_count bigint)\n LANGUAGE sql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n    SELECT\r\n        e.id AS exam_id,\r\n        e.exam_template_id,\r\n        e.grade_level,\r\n        e.score,\r\n        e.total_questions,\r\n\r\n        round(\r\n            (e.score::numeric * 100) /\r\n            nullif(e.total_questions,0),\r\n            2\r\n        ) AS accuracy,\r\n\r\n        e.exp_earned,\r\n        e.created_at,\r\n        e.submitted_at,\r\n\r\n        CASE\r\n            WHEN e.submitted_at IS NOT NULL\r\n            THEN extract(epoch FROM (e.submitted_at - e.created_at))::integer\r\n            ELSE NULL\r\n        END AS duration_seconds,\r\n\r\n        CASE\r\n            WHEN e.submitted_at IS NULL THEN 'in_progress'\r\n            ELSE 'completed'\r\n        END AS exam_status,\r\n\r\n        e.is_active,\r\n\r\n        count(*) OVER() AS total_count\r\n\r\n    FROM exams e\r\n    WHERE e.user_id = p_user_id\r\n\r\n    ORDER BY e.created_at DESC\r\n    LIMIT p_limit\r\n    OFFSET p_offset;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_hard_delete_question",
    "identity_arguments": "p_question_id bigint",
    "arguments": "p_question_id bigint",
    "return_type": "void",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": false,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_hard_delete_question(p_question_id bigint)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_usage_count integer;\r\nBEGIN\r\n  SELECT COUNT(*) INTO v_usage_count\r\n  FROM exam_questions\r\n  WHERE original_question_id = p_question_id;\r\n\r\n  IF v_usage_count > 0 THEN\r\n    RAISE EXCEPTION \r\n      'QUESTION_ALREADY_USED_IN_EXAMS (%). HARD DELETE NOT ALLOWED',\r\n      v_usage_count;\r\n  END IF;\r\n\r\n  DELETE FROM questions\r\n  WHERE id = p_question_id;\r\n\r\n  IF NOT FOUND THEN\r\n    RAISE EXCEPTION 'QUESTION_NOT_FOUND';\r\n  END IF;\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_list_questions_v2",
    "identity_arguments": "p_limit integer, p_offset integer, p_search text, p_is_active boolean",
    "arguments": "p_limit integer, p_offset integer, p_search text DEFAULT NULL::text, p_is_active boolean DEFAULT NULL::boolean",
    "return_type": "TABLE(question_id bigint, question_text text, difficulty smallint, grade_level integer, category_id bigint, passage_id bigint, passage_title text, question_type_code text, tag_ids bigint[], option_count integer, answer_count integer, is_active boolean, created_at timestamp with time zone, total_count bigint)",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": false,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_list_questions_v2(p_limit integer, p_offset integer, p_search text DEFAULT NULL::text, p_is_active boolean DEFAULT NULL::boolean)\n RETURNS TABLE(question_id bigint, question_text text, difficulty smallint, grade_level integer, category_id bigint, passage_id bigint, passage_title text, question_type_code text, tag_ids bigint[], option_count integer, answer_count integer, is_active boolean, created_at timestamp with time zone, total_count bigint)\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  RETURN QUERY\r\n  WITH filtered AS (\r\n    SELECT q.*\r\n    FROM questions q\r\n    WHERE \r\n      -- 🔴 QUAN TRỌNG: loại bỏ soft delete\r\n      q.deleted_at IS NULL\r\n      \r\n      -- search\r\n      AND (\r\n        p_search IS NULL \r\n        OR q.question_text ILIKE '%' || p_search || '%'\r\n      )\r\n      \r\n      -- filter is_active\r\n      AND (\r\n        p_is_active IS NULL \r\n        OR q.is_active = p_is_active\r\n      )\r\n  ),\r\n  counted AS (\r\n    SELECT COUNT(*) AS total FROM filtered\r\n  )\r\n  SELECT\r\n    q.id,\r\n    q.question_text,\r\n    q.difficulty,\r\n    q.grade_level,\r\n    q.category_id,\r\n    q.passage_id,\r\n    p.title,\r\n    qt.code,\r\n    ARRAY[]::bigint[], -- giữ nguyên logic tag nếu bạn đã có join riêng\r\n    0, -- option_count\r\n    0, -- answer_count\r\n    q.is_active,\r\n    q.created_at,\r\n    counted.total\r\n  FROM filtered q\r\n  LEFT JOIN passages p ON p.id = q.passage_id\r\n  LEFT JOIN question_types qt ON qt.id = q.question_type_id\r\n  CROSS JOIN counted\r\n  ORDER BY q.created_at DESC\r\n  LIMIT p_limit\r\n  OFFSET p_offset;\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_preview_exam_template",
    "identity_arguments": "p_template_id bigint",
    "arguments": "p_template_id bigint",
    "return_type": "TABLE(section_order integer, item_order integer, template_item_id bigint, question_id bigint, question_text text, difficulty integer, passage_id bigint, passage_text text)",
    "language": "plpgsql",
    "provolatile": "s",
    "is_security_definer": false,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_preview_exam_template(p_template_id bigint)\n RETURNS TABLE(section_order integer, item_order integer, template_item_id bigint, question_id bigint, question_text text, difficulty integer, passage_id bigint, passage_text text)\n LANGUAGE plpgsql\n STABLE\nAS $function$\r\ndeclare\r\n    v_missing_items integer;\r\nbegin\r\n\r\n    perform rpc_assert_min_role('teacher');\r\n\r\n    /* =========================================\r\n       VALIDATION 1: TEMPLATE EXIST\r\n    ========================================= */\r\n\r\n    if not exists (\r\n        select 1\r\n        from exam_templates\r\n        where id = p_template_id\r\n    ) then\r\n        raise exception 'Template % does not exist', p_template_id;\r\n    end if;\r\n\r\n    /* =========================================\r\n       VALIDATION 2: CHECK QUESTION / PASSAGE POOL\r\n    ========================================= */\r\n\r\n    select count(*)\r\n    into v_missing_items\r\n    from exam_template_items eti\r\n    join exam_template_sections ets\r\n        on ets.id = eti.template_section_id\r\n    where ets.exam_template_id = p_template_id\r\n    and (\r\n        case\r\n            -- PASSAGE TYPES\r\n            when eti.question_type_id in (\r\n                select id from question_types where code in ('C1','C3')\r\n            )\r\n            then (\r\n                select count(*)\r\n                from passages p\r\n                where exists (\r\n                    select 1\r\n                    from questions q\r\n                    where\r\n                        q.passage_id = p.id\r\n                        and q.category_id = eti.category_id\r\n                        and q.question_type_id = eti.question_type_id\r\n                        and (\r\n                            eti.difficulty_min is null\r\n                            or eti.difficulty_max is null\r\n                            or q.difficulty between eti.difficulty_min and eti.difficulty_max\r\n                        )\r\n                        and q.is_active = true\r\n                        and q.deleted_at is null\r\n                )\r\n            )\r\n\r\n            -- QUESTION TYPES\r\n            else (\r\n                select count(*)\r\n                from questions q\r\n                where\r\n                    q.category_id = eti.category_id\r\n                    and q.question_type_id = eti.question_type_id\r\n                    and (\r\n                        eti.difficulty_min is null\r\n                        or eti.difficulty_max is null\r\n                        or q.difficulty between eti.difficulty_min and eti.difficulty_max\r\n                    )\r\n                    and q.is_active = true\r\n                    and q.deleted_at is null\r\n            )\r\n        end\r\n    ) < eti.question_count;\r\n\r\n    if v_missing_items > 0 then\r\n        raise exception 'Template has items without enough questions';\r\n    end if;\r\n\r\n    /* =========================================\r\n       RETURN PREVIEW\r\n    ========================================= */\r\n\r\n    return query\r\n\r\n    /* ================================\r\n       QUESTION BASED TYPES\r\n    ================================ */\r\n\r\n    select\r\n        ets.section_order,\r\n        eti.item_order,\r\n        eti.id,\r\n        q.id,\r\n        q.question_text,\r\n        q.difficulty,\r\n        q.passage_id,\r\n        p.content\r\n\r\n    from exam_template_items eti\r\n\r\n    join exam_template_sections ets\r\n        on ets.id = eti.template_section_id\r\n\r\n    join exam_templates et\r\n        on et.id = ets.exam_template_id\r\n\r\n    join question_types qt\r\n        on qt.id = eti.question_type_id\r\n\r\n    cross join lateral (\r\n\r\n        select\r\n            q.id,\r\n            q.question_text,\r\n            q.difficulty,\r\n            q.passage_id\r\n\r\n        from questions q\r\n\r\n        where\r\n            q.category_id = eti.category_id\r\n            and q.question_type_id = eti.question_type_id\r\n            and (\r\n                eti.difficulty_min is null\r\n                or eti.difficulty_max is null\r\n                or q.difficulty between eti.difficulty_min and eti.difficulty_max\r\n            )\r\n            and q.is_active = true\r\n            and q.deleted_at is null\r\n\r\n        order by random()\r\n        limit eti.question_count\r\n\r\n    ) q\r\n\r\n    left join passages p\r\n        on p.id = q.passage_id\r\n\r\n    where\r\n        et.id = p_template_id\r\n        and qt.code not in ('C1','C3')\r\n\r\n\r\n    union all\r\n\r\n\r\n    /* ================================\r\n       PASSAGE BASED TYPES\r\n    ================================ */\r\n\r\n    select\r\n        ets.section_order,\r\n        eti.item_order,\r\n        eti.id,\r\n        q.id,\r\n        q.question_text,\r\n        q.difficulty,\r\n        q.passage_id,\r\n        p.content\r\n\r\n    from exam_template_items eti\r\n\r\n    join exam_template_sections ets\r\n        on ets.id = eti.template_section_id\r\n\r\n    join exam_templates et\r\n        on et.id = ets.exam_template_id\r\n\r\n    join question_types qt\r\n        on qt.id = eti.question_type_id\r\n\r\n    cross join lateral (\r\n\r\n        select p.id\r\n        from passages p\r\n\r\n        where exists (\r\n            select 1\r\n            from questions q\r\n            where\r\n                q.passage_id = p.id\r\n                and q.category_id = eti.category_id\r\n                and q.question_type_id = eti.question_type_id\r\n                and (\r\n                    eti.difficulty_min is null\r\n                    or eti.difficulty_max is null\r\n                    or q.difficulty between eti.difficulty_min and eti.difficulty_max\r\n                )\r\n                and q.is_active = true\r\n                and q.deleted_at is null\r\n        )\r\n\r\n        order by random()\r\n        limit eti.question_count\r\n\r\n    ) rp\r\n\r\n    cross join lateral (\r\n\r\n        select\r\n            q.id,\r\n            q.question_text,\r\n            q.difficulty,\r\n            q.passage_id\r\n        from questions q\r\n        where\r\n            q.passage_id = rp.id\r\n            and q.is_active = true\r\n            and q.deleted_at is null\r\n        order by random()\r\n        limit 1\r\n\r\n    ) q\r\n\r\n    left join passages p\r\n        on p.id = q.passage_id\r\n\r\n    where\r\n        et.id = p_template_id\r\n        and qt.code in ('C1','C3')\r\n\r\n\r\n    order by\r\n        section_order,\r\n        item_order,\r\n        template_item_id,\r\n        question_id;\r\n\r\nend;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_purge_deleted_exams",
    "identity_arguments": "p_limit integer",
    "arguments": "p_limit integer DEFAULT 500",
    "return_type": "TABLE(deleted_exams integer)",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_purge_deleted_exams(p_limit integer DEFAULT 500)\n RETURNS TABLE(deleted_exams integer)\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_deleted_count integer;\r\n\r\nBEGIN\r\n\t\r\n\tperform rpc_assert_min_role('admin');\r\n\r\n    -- ===============================\r\n    -- Permission guard (admin only)\r\n    -- ===============================\r\n    IF auth.role() <> 'service_role' THEN\r\n        RAISE EXCEPTION 'permission denied';\r\n    END IF;\r\n\r\n\r\n    -- ===============================\r\n    -- Batch purge\r\n    -- ===============================\r\n    WITH exams_to_delete AS (\r\n        SELECT id\r\n        FROM exams\r\n        WHERE deleted_at IS NOT NULL\r\n        AND deleted_at < now() - interval '7 days'\r\n        ORDER BY deleted_at\r\n        LIMIT p_limit\r\n        FOR UPDATE SKIP LOCKED\r\n    ),\r\n\r\n    -- ===============================\r\n    -- Delete user answers\r\n    -- ===============================\r\n    deleted_answers AS (\r\n        DELETE FROM user_answers ua\r\n        USING exams_to_delete e\r\n        WHERE ua.exam_id = e.id\r\n    ),\r\n\r\n    -- ===============================\r\n    -- Delete exam question options\r\n    -- ===============================\r\n    deleted_options AS (\r\n        DELETE FROM exam_question_options eqo\r\n        USING exam_questions eq, exams_to_delete e\r\n        WHERE eqo.exam_question_id = eq.id\r\n        AND eq.exam_id = e.id\r\n    ),\r\n\r\n    -- ===============================\r\n    -- Delete exam questions\r\n    -- ===============================\r\n    deleted_questions AS (\r\n        DELETE FROM exam_questions eq\r\n        USING exams_to_delete e\r\n        WHERE eq.exam_id = e.id\r\n    ),\r\n\r\n    -- ===============================\r\n    -- Delete exam passages\r\n    -- ===============================\r\n    deleted_passages AS (\r\n        DELETE FROM exam_passages ep\r\n        USING exams_to_delete e\r\n        WHERE ep.exam_id = e.id\r\n    ),\r\n\r\n    -- ===============================\r\n    -- Delete exams\r\n    -- ===============================\r\n    deleted_exams AS (\r\n        DELETE FROM exams ex\r\n        USING exams_to_delete e\r\n        WHERE ex.id = e.id\r\n        RETURNING ex.id\r\n    )\r\n\r\n    SELECT count(*)\r\n    INTO v_deleted_count\r\n    FROM deleted_exams;\r\n\r\n\r\n    RETURN QUERY\r\n    SELECT v_deleted_count;\r\n\r\nEND;\r\n\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_question_accuracy",
    "identity_arguments": "p_student_id uuid",
    "arguments": "p_student_id uuid",
    "return_type": "TABLE(question_type text, accuracy numeric)",
    "language": "sql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_question_accuracy(p_student_id uuid)\n RETURNS TABLE(question_type text, accuracy numeric)\n LANGUAGE sql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nSELECT\r\n    qt.code AS question_type,\r\n    ROUND(AVG(ua.is_correct::int) * 100, 2) AS accuracy\r\nFROM user_answers ua\r\nJOIN exams e\r\n    ON e.id = ua.exam_id\r\nJOIN exam_questions eq\r\n    ON eq.id = ua.exam_question_id\r\nJOIN question_types qt\r\n    ON qt.id = eq.question_type_snapshot\r\nWHERE e.user_id = p_student_id\r\n  AND e.submitted_at IS NOT NULL\r\nGROUP BY qt.code\r\nORDER BY qt.code;\r\n\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_restore_deleted_exam",
    "identity_arguments": "p_exam_id bigint",
    "arguments": "p_exam_id bigint",
    "return_type": "TABLE(success boolean, restored_exam_id bigint)",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_restore_deleted_exam(p_exam_id bigint)\n RETURNS TABLE(success boolean, restored_exam_id bigint)\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exam_id bigint;\r\n    v_deleted_at timestamptz;\r\n\r\nBEGIN\r\n\t\r\n\tperform rpc_assert_min_role('admin');\r\n\t\r\n    -- Lock exam row and fetch state\r\n    SELECT id, deleted_at\r\n    INTO v_exam_id, v_deleted_at\r\n    FROM exams\r\n    WHERE id = p_exam_id\r\n    FOR UPDATE;\r\n\r\n    -- Exam does not exist\r\n    IF v_exam_id IS NULL THEN\r\n        RAISE EXCEPTION 'Exam not found';\r\n    END IF;\r\n\r\n    -- Exam is not soft deleted\r\n    IF v_deleted_at IS NULL THEN\r\n        RAISE EXCEPTION 'Exam is not deleted';\r\n    END IF;\r\n\r\n    -- Restore exam\r\n    UPDATE exams\r\n    SET\r\n        deleted_at = NULL,\r\n        updated_at = now()\r\n    WHERE id = v_exam_id;\r\n\r\n    RETURN QUERY\r\n    SELECT true, v_exam_id;\r\n\r\nEND;\r\n\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_resume_exam",
    "identity_arguments": "",
    "arguments": "",
    "return_type": "jsonb",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_resume_exam()\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exam record;\r\n    v_now timestamptz := now();\r\n    v_end_time timestamptz;\r\n    v_remaining integer;\r\nBEGIN\r\n\r\n    ------------------------------------------------\r\n    -- 0️⃣ Auth\r\n    ------------------------------------------------\r\n    PERFORM public.rpc_assert_min_role('student');\r\n\r\n    ------------------------------------------------\r\n    -- 1️⃣ Find latest active exam + LOCK\r\n    ------------------------------------------------\r\n    SELECT *\r\n    INTO v_exam\r\n    FROM exams\r\n    WHERE user_id = auth.uid()\r\n      AND submitted_at IS NULL\r\n      AND is_active = true\r\n      AND deleted_at IS NULL\r\n    ORDER BY created_at DESC\r\n    LIMIT 1\r\n    FOR UPDATE;\r\n\r\n    IF NOT FOUND THEN\r\n        RETURN jsonb_build_object(\r\n            'status','no_exam'\r\n        );\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 2️⃣ Compute end_time\r\n    ------------------------------------------------\r\n    v_end_time :=\r\n        v_exam.created_at\r\n        + (v_exam.duration_minutes * interval '1 minute');\r\n\r\n    ------------------------------------------------\r\n    -- 3️⃣ Auto submit if expired (SELF-HEALING)\r\n    ------------------------------------------------\r\n    IF v_now >= v_end_time THEN\r\n\r\n        PERFORM public.rpc_submit_exam(v_exam.id);\r\n\r\n        RETURN jsonb_build_object(\r\n            'status','submitted',\r\n            'exam_id',v_exam.id,\r\n            'end_time',v_end_time\r\n        );\r\n\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 4️⃣ Remaining time\r\n    ------------------------------------------------\r\n    v_remaining :=\r\n        GREATEST(\r\n            FLOOR(EXTRACT(EPOCH FROM (v_end_time - v_now))),\r\n            0\r\n        )::integer;\r\n\r\n    ------------------------------------------------\r\n    -- 5️⃣ Return running state\r\n    ------------------------------------------------\r\n    RETURN jsonb_build_object(\r\n        'status','running',\r\n        'exam_id',v_exam.id,\r\n        'remaining_seconds',v_remaining,\r\n        'end_time',v_end_time,\r\n        'total_questions',v_exam.total_questions\r\n    );\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_save_answer",
    "identity_arguments": "p_exam_id bigint, p_exam_question_id bigint, p_selected_option_id bigint, p_text_answer text",
    "arguments": "p_exam_id bigint, p_exam_question_id bigint, p_selected_option_id bigint DEFAULT NULL::bigint, p_text_answer text DEFAULT NULL::text",
    "return_type": "void",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_save_answer(p_exam_id bigint, p_exam_question_id bigint, p_selected_option_id bigint DEFAULT NULL::bigint, p_text_answer text DEFAULT NULL::text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exam record;\r\n    v_question_exists boolean;\r\n    v_option_valid boolean;\r\n    v_end_time timestamptz;\r\n    v_now timestamptz := now();\r\nBEGIN\r\n\r\n    ------------------------------------------------\r\n    -- 0️⃣ Auth\r\n    ------------------------------------------------\r\n    PERFORM public.rpc_assert_min_role('student');\r\n\r\n    ------------------------------------------------\r\n    -- 1️⃣ Lock exam\r\n    ------------------------------------------------\r\n    SELECT *\r\n    INTO v_exam\r\n    FROM public.exams\r\n    WHERE id = p_exam_id\r\n      AND user_id = auth.uid()\r\n      AND submitted_at IS NULL\r\n      AND is_active = true\r\n      AND deleted_at IS NULL\r\n    FOR UPDATE;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'Exam not found or already submitted';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 2️⃣ Compute end_time\r\n    ------------------------------------------------\r\n    v_end_time :=\r\n        v_exam.created_at\r\n        + (v_exam.duration_minutes * interval '1 minute');\r\n\r\n    ------------------------------------------------\r\n    -- 3️⃣ Auto submit if expired (SELF-HEALING)\r\n    ------------------------------------------------\r\n    IF v_now >= v_end_time THEN\r\n\r\n        PERFORM public.rpc_submit_exam(p_exam_id);\r\n\r\n        RAISE EXCEPTION 'Exam time is over';\r\n\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 4️⃣ Check question belongs to exam\r\n    ------------------------------------------------\r\n    SELECT EXISTS (\r\n        SELECT 1\r\n        FROM public.exam_questions\r\n        WHERE id = p_exam_question_id\r\n          AND exam_id = p_exam_id\r\n    )\r\n    INTO v_question_exists;\r\n\r\n    IF NOT v_question_exists THEN\r\n        RAISE EXCEPTION 'Question does not belong to this exam';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 5️⃣ Validate option\r\n    ------------------------------------------------\r\n    IF p_selected_option_id IS NOT NULL THEN\r\n\r\n        SELECT EXISTS (\r\n            SELECT 1\r\n            FROM public.exam_question_options\r\n            WHERE id = p_selected_option_id\r\n              AND exam_question_id = p_exam_question_id\r\n        )\r\n        INTO v_option_valid;\r\n\r\n        IF NOT v_option_valid THEN\r\n            RAISE EXCEPTION 'Invalid option';\r\n        END IF;\r\n\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 6️⃣ UPSERT answer\r\n    ------------------------------------------------\r\n    INSERT INTO public.user_answers (\r\n        exam_id,\r\n        exam_question_id,\r\n        exam_question_option_id,\r\n        answer_text,\r\n        answered_at,\r\n        user_id\r\n    )\r\n    VALUES (\r\n        p_exam_id,\r\n        p_exam_question_id,\r\n        p_selected_option_id,\r\n        p_text_answer,\r\n        v_now,\r\n        auth.uid()\r\n    )\r\n    ON CONFLICT (exam_id, exam_question_id)\r\n    DO UPDATE SET\r\n        exam_question_option_id = EXCLUDED.exam_question_option_id,\r\n        answer_text = EXCLUDED.answer_text,\r\n        answered_at = v_now;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_soft_delete_exam",
    "identity_arguments": "p_exam_id bigint",
    "arguments": "p_exam_id bigint",
    "return_type": "TABLE(success boolean, deleted_exam_id bigint)",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_soft_delete_exam(p_exam_id bigint)\n RETURNS TABLE(success boolean, deleted_exam_id bigint)\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exam_id bigint;\r\n    v_deleted_at timestamptz;\r\n    v_submitted_at timestamptz;\r\n    v_role text;\r\n\r\nBEGIN\r\n\r\n    ------------------------------------------------\r\n    -- Authorization\r\n    ------------------------------------------------\r\n    SELECT role\r\n    INTO v_role\r\n    FROM profiles\r\n    WHERE id = auth.uid();\r\n\r\n    IF v_role NOT IN ('teacher','admin') THEN\r\n        RAISE EXCEPTION 'Permission denied';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- Lock exam\r\n    ------------------------------------------------\r\n    SELECT id, deleted_at, submitted_at\r\n    INTO v_exam_id, v_deleted_at, v_submitted_at\r\n    FROM exams\r\n    WHERE id = p_exam_id\r\n    FOR UPDATE;\r\n\r\n    IF v_exam_id IS NULL THEN\r\n        RAISE EXCEPTION 'Exam not found';\r\n    END IF;\r\n\r\n    IF v_deleted_at IS NOT NULL THEN\r\n        RAISE EXCEPTION 'Exam already deleted';\r\n    END IF;\r\n\r\n    IF v_submitted_at IS NOT NULL THEN\r\n        RAISE EXCEPTION 'Submitted exam cannot be deleted';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- Delete answers\r\n    ------------------------------------------------\r\n    DELETE FROM user_answers\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Delete option snapshots\r\n    ------------------------------------------------\r\n    DELETE FROM exam_question_options\r\n    WHERE exam_question_id IN (\r\n        SELECT id\r\n        FROM exam_questions\r\n        WHERE exam_id = v_exam_id\r\n    );\r\n\r\n    ------------------------------------------------\r\n    -- Delete question snapshots\r\n    ------------------------------------------------\r\n    DELETE FROM exam_questions\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Delete passage snapshots\r\n    ------------------------------------------------\r\n    DELETE FROM exam_passages\r\n    WHERE exam_id = v_exam_id;\r\n\r\n    ------------------------------------------------\r\n    -- Soft delete exam\r\n    ------------------------------------------------\r\n    UPDATE exams\r\n    SET\r\n        deleted_at = now(),\r\n        updated_at = now()\r\n    WHERE id = v_exam_id;\r\n\r\n    RETURN QUERY\r\n    SELECT true, v_exam_id;\r\n\r\nEND;\r\n\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_soft_delete_question",
    "identity_arguments": "p_question_id bigint",
    "arguments": "p_question_id bigint",
    "return_type": "void",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": false,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_soft_delete_question(p_question_id bigint)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  UPDATE questions\r\n  SET deleted_at = now()\r\n  WHERE id = p_question_id\r\n    AND deleted_at IS NULL;\r\n\r\n  IF NOT FOUND THEN\r\n    RAISE EXCEPTION 'QUESTION_NOT_FOUND_OR_ALREADY_DELETED';\r\n  END IF;\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_start_exam",
    "identity_arguments": "p_template_id bigint",
    "arguments": "p_template_id bigint",
    "return_type": "bigint",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_start_exam(p_template_id bigint)\n RETURNS bigint\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\ndeclare\r\n\r\n    v_user_id uuid;\r\n    v_exam_id bigint;\r\n\r\n    v_grade_level integer;\r\n    v_duration_minutes integer;\r\n\r\n    v_total_questions integer := 0;\r\n\r\n    v_item record;\r\n    v_question record;\r\n    v_passage record;\r\n\r\n    v_exam_question_id bigint;\r\n\r\n    v_available_count integer;\r\n\r\nbegin\r\n\t\r\n\tperform public.rpc_assert_min_role('student');\r\n\r\n    ------------------------------------------------\r\n    -- current user\r\n    ------------------------------------------------\r\n\r\n    v_user_id := auth.uid();\r\n\r\n    if v_user_id is null then\r\n        raise exception 'User not authenticated';\r\n    end if;\r\n\r\n\r\n    ------------------------------------------------\r\n    -- anti race condition\r\n    ------------------------------------------------\r\n\r\n    perform pg_advisory_xact_lock(hashtext(v_user_id::text));\r\n\r\n\r\n    ------------------------------------------------\r\n    -- validate template\r\n    ------------------------------------------------\r\n\r\n    select\r\n        grade_level,\r\n        duration_minutes\r\n    into\r\n        v_grade_level,\r\n        v_duration_minutes\r\n    from exam_templates\r\n    where id = p_template_id\r\n    and is_active = true\r\n    and deleted_at is null;\r\n\r\n    if not found then\r\n        raise exception 'Template not available';\r\n    end if;\r\n\r\n\r\n    ------------------------------------------------\r\n    -- prevent multiple active exams\r\n    ------------------------------------------------\r\n\r\n    if exists (\r\n        select 1\r\n        from exams\r\n        where user_id = v_user_id\r\n        and submitted_at is null\r\n        and is_active = true\r\n        and deleted_at is null\r\n    ) then\r\n        raise exception 'User already has active exam';\r\n    end if;\r\n\r\n\r\n    ------------------------------------------------\r\n    -- pre check question availability\r\n    ------------------------------------------------\r\n\r\n    for v_item in\r\n    select eti.*\r\n    from exam_template_items eti\r\n    join exam_template_sections ets\r\n    on ets.id = eti.template_section_id\r\n    where ets.exam_template_id = p_template_id\r\norder by ets.section_order, eti.item_order\r\nloop\r\n\r\n        ------------------------------------------------\r\n        -- single questions\r\n        ------------------------------------------------\r\n\r\n        if v_item.question_count is not null then\r\n\r\n            select count(*)\r\n            into v_available_count\r\n            from questions q\r\n            where q.category_id = v_item.category_id\r\n            and q.question_type_id = v_item.question_type_id\r\n            and q.grade_level = v_grade_level\r\n            and q.passage_id is null\r\n            and q.is_active = true\r\n            and q.deleted_at is null;\r\n\r\n            if v_available_count < v_item.question_count then\r\n                raise exception\r\n                'Not enough standalone questions for category %',\r\n                v_item.category_id;\r\n            end if;\r\n\r\n        end if;\r\n\r\n\r\n        ------------------------------------------------\r\n        -- passage questions\r\n        ------------------------------------------------\r\n\r\n        if v_item.passage_count is not null then\r\n\r\n            select count(*)\r\n            into v_available_count\r\n            from passages p\r\n            where p.is_active = true\r\n            and p.deleted_at is null;\r\n\r\n            if v_available_count < v_item.passage_count then\r\n                raise exception 'Not enough passages';\r\n            end if;\r\n\r\n        end if;\r\n\r\n    end loop;\r\n\r\n\r\n    ------------------------------------------------\r\n    -- create exam\r\n    ------------------------------------------------\r\n\r\n    insert into exams (\r\n        user_id,\r\n        grade_level,\r\n        exam_template_id,\r\n        total_questions,\r\n        duration_minutes\r\n    )\r\n    values (\r\n        v_user_id,\r\n        v_grade_level,\r\n        p_template_id,\r\n        0,\r\n        v_duration_minutes\r\n    )\r\n    returning id into v_exam_id;\r\n\r\n\r\n    ------------------------------------------------\r\n    -- generate exam\r\n    ------------------------------------------------\r\n\r\n    for v_item in\r\n    select eti.*\r\n    from exam_template_items eti\r\n    join exam_template_sections ets\r\n    on ets.id = eti.template_section_id\r\n    where ets.exam_template_id = p_template_id\r\norder by ets.section_order, eti.item_order\r\nloop\r\n\r\n        ------------------------------------------------\r\n        -- standalone questions\r\n        ------------------------------------------------\r\n\r\n        if v_item.question_count is not null then\r\n\r\n            for v_question in\r\n                select q.*\r\n                from questions q\r\n                where q.category_id = v_item.category_id\r\n                and q.question_type_id = v_item.question_type_id\r\n                and q.grade_level = v_grade_level\r\n                and q.passage_id is null\r\n                and q.is_active = true\r\n                and q.deleted_at is null\r\n                order by random()\r\n                limit v_item.question_count\r\n            loop\r\n\r\n                v_total_questions := v_total_questions + 1;\r\n\r\n                insert into exam_questions(\r\n                    exam_id,\r\n                    question_order,\r\n                    original_question_id,\r\n                    question_text_snapshot,\r\n                    explanation_snapshot,\r\n                    question_type_snapshot,\r\n                    difficulty_snapshot\r\n                )\r\n                values (\r\n                    v_exam_id,\r\n                    v_total_questions,\r\n                    v_question.id,\r\n                    v_question.question_text,\r\n                    v_question.explanation,\r\n                    v_question.question_type_id,\r\n                    v_question.difficulty\r\n                )\r\n                returning id into v_exam_question_id;\r\n\r\n\r\n                insert into exam_question_options(\r\n                    exam_question_id,\r\n                    option_label_snapshot,\r\n                    option_text_snapshot,\r\n                    is_correct_snapshot,\r\n                    option_order\r\n                )\r\n                select\r\n                    v_exam_question_id,\r\n                    o.option_label,\r\n                    o.option_text,\r\n                    o.is_correct,\r\n                    row_number() over(order by random())\r\n                from options o\r\n                where o.question_id = v_question.id;\r\n\r\n            end loop;\r\n\r\n        end if;\r\n\r\n\r\n        ------------------------------------------------\r\n        -- passage questions\r\n        ------------------------------------------------\r\n\r\n        if v_item.passage_count is not null then\r\n\r\n            for v_passage in\r\n                select *\r\n                from passages p\r\n                where p.is_active = true\r\n                and p.deleted_at is null\r\n                order by random()\r\n                limit v_item.passage_count\r\n            loop\r\n\r\n                for v_question in\r\n                    select *\r\n                    from questions q\r\n                    where q.passage_id = v_passage.id\r\n                    and q.is_active = true\r\n                    and q.deleted_at is null\r\n                loop\r\n\r\n                    v_total_questions := v_total_questions + 1;\r\n\r\n                    insert into exam_questions(\r\n                        exam_id,\r\n                        question_order,\r\n                        original_question_id,\r\n                        question_text_snapshot,\r\n                        explanation_snapshot,\r\n                        question_type_snapshot,\r\n                        difficulty_snapshot\r\n                    )\r\n                    values (\r\n                        v_exam_id,\r\n                        v_total_questions,\r\n                        v_question.id,\r\n                        v_question.question_text,\r\n                        v_question.explanation,\r\n                        v_question.question_type_id,\r\n                        v_question.difficulty\r\n                    )\r\n                    returning id into v_exam_question_id;\r\n\r\n\r\n                    insert into exam_question_options(\r\n                        exam_question_id,\r\n                        option_label_snapshot,\r\n                        option_text_snapshot,\r\n                        is_correct_snapshot,\r\n                        option_order\r\n                    )\r\n                    select\r\n                        v_exam_question_id,\r\n                        o.option_label,\r\n                        o.option_text,\r\n                        o.is_correct,\r\n                        row_number() over(order by random())\r\n                    from options o\r\n                    where o.question_id = v_question.id;\r\n\r\n                end loop;\r\n\r\n            end loop;\r\n\r\n        end if;\r\n\r\n    end loop;\r\n\r\n\r\n    ------------------------------------------------\r\n    -- update total question count\r\n    ------------------------------------------------\r\n\r\n    update exams\r\n    set total_questions = v_total_questions\r\n    where id = v_exam_id;\r\n\r\n\r\n    return v_exam_id;\r\n\r\nend;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_submit_exam",
    "identity_arguments": "p_exam_id bigint",
    "arguments": "p_exam_id bigint",
    "return_type": "jsonb",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_submit_exam(p_exam_id bigint)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\ndeclare\r\n    v_exam record;\r\n    v_correct integer;\r\n    v_now timestamptz := now();\r\n    v_end_time timestamptz;\r\nbegin\r\n\t\r\n\tperform public.rpc_assert_min_role('student');\r\n\r\n    -- 1️⃣ Lock exam\r\n    select *\r\n    into v_exam\r\n    from exams\r\n    where id = p_exam_id\r\n    for update;\r\n\r\n    if not found then\r\n        raise exception 'Exam not found';\r\n    end if;\r\n\r\n    -- 2️⃣ Ownership check\r\n    if v_exam.user_id <> auth.uid() then\r\n        raise exception 'Not your exam';\r\n    end if;\r\n\r\n    -- 3️⃣ Prevent double submit\r\n    if v_exam.submitted_at is not null then\r\n        raise exception 'Exam already submitted';\r\n    end if;\r\n\r\n    -- 4️⃣ Calculate end time\r\n    v_end_time :=\r\n        v_exam.created_at\r\n        + (v_exam.duration_minutes || ' minutes')::interval;\r\n\r\n    -- 5️⃣ Prevent submit before start\r\n    if v_now < v_exam.created_at then\r\n        raise exception 'Exam has not started';\r\n    end if;\r\n\r\n    -- 6️⃣ Update correctness snapshot\r\n    update user_answers ua\r\n    set is_correct = eqo.is_correct_snapshot\r\n    from exam_question_options eqo\r\n    where ua.exam_question_option_id = eqo.id\r\n    and ua.exam_id = p_exam_id\r\n    and ua.is_correct is null;\r\n\r\n    -- 7️⃣ Count correct answers\r\n    select count(*)\r\n    into v_correct\r\n    from user_answers\r\n    where exam_id = p_exam_id\r\n    and is_correct = true;\r\n\r\n    -- 8️⃣ Update exam result\r\n    update exams\r\n    set\r\n        submitted_at = v_now,\r\n        score = v_correct,\r\n        updated_at = v_now\r\n    where id = p_exam_id;\r\n\r\n    -- 9️⃣ Return result\r\n    return jsonb_build_object(\r\n        'correct', v_correct,\r\n        'total', v_exam.total_questions,\r\n        'end_time', v_end_time,\r\n        'submitted_at', v_now\r\n    );\r\n\r\nend;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_update_exam_template",
    "identity_arguments": "p_template_id bigint, p_name text, p_grade_level integer, p_duration_minutes integer",
    "arguments": "p_template_id bigint, p_name text, p_grade_level integer, p_duration_minutes integer",
    "return_type": "void",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_update_exam_template(p_template_id bigint, p_name text, p_grade_level integer, p_duration_minutes integer)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_exists boolean;\r\nBEGIN\r\n\t\r\n\tperform rpc_assert_min_role('teacher');\r\n\t\r\n    ----------------------------------------------------------------\r\n    -- 1️⃣ Validate template tồn tại và chưa bị soft delete\r\n    ----------------------------------------------------------------\r\n\r\n    SELECT EXISTS (\r\n        SELECT 1\r\n        FROM exam_templates\r\n        WHERE id = p_template_id\r\n        AND deleted_at IS NULL\r\n    )\r\n    INTO v_exists;\r\n\r\n    IF NOT v_exists THEN\r\n        RAISE EXCEPTION 'Exam template not found';\r\n    END IF;\r\n\r\n\r\n    ----------------------------------------------------------------\r\n    -- 2️⃣ Không cho sửa nếu còn exam chưa submit\r\n    ----------------------------------------------------------------\r\n\r\n    IF EXISTS (\r\n        SELECT 1\r\n        FROM exams\r\n        WHERE template_id = p_template_id\r\n        AND submitted_at IS NULL\r\n    ) THEN\r\n        RAISE EXCEPTION\r\n        'Cannot modify template while exams are still in progress';\r\n    END IF;\r\n\r\n\r\n    ----------------------------------------------------------------\r\n    -- 3️⃣ Validate name\r\n    ----------------------------------------------------------------\r\n\r\n    IF p_name IS NULL OR trim(p_name) = '' THEN\r\n        RAISE EXCEPTION 'Template name cannot be empty';\r\n    END IF;\r\n\r\n\r\n    ----------------------------------------------------------------\r\n    -- 4️⃣ Validate grade level\r\n    ----------------------------------------------------------------\r\n\r\n    IF p_grade_level < 6 OR p_grade_level > 12 THEN\r\n        RAISE EXCEPTION 'Grade level must be between 6 and 12';\r\n    END IF;\r\n\r\n\r\n    ----------------------------------------------------------------\r\n    -- 5️⃣ Validate duration\r\n    ----------------------------------------------------------------\r\n\r\n    IF p_duration_minutes < 5 OR p_duration_minutes > 150 THEN\r\n        RAISE EXCEPTION 'Duration must be between 5 and 150 minutes';\r\n    END IF;\r\n\r\n\r\n    ----------------------------------------------------------------\r\n    -- 6️⃣ Update template\r\n    ----------------------------------------------------------------\r\n\r\n    UPDATE exam_templates\r\n    SET\r\n        name = trim(p_name),\r\n        grade_level = p_grade_level,\r\n        duration_minutes = p_duration_minutes,\r\n        updated_at = now()\r\n    WHERE id = p_template_id;\r\n\r\nEND;\r\n\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_update_passage_with_questions",
    "identity_arguments": "p_passage_id bigint, p_content text, p_audio_url text, p_questions jsonb",
    "arguments": "p_passage_id bigint, p_content text, p_audio_url text, p_questions jsonb",
    "return_type": "bigint",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_update_passage_with_questions(p_passage_id bigint, p_content text, p_audio_url text, p_questions jsonb)\n RETURNS bigint\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\nDECLARE\r\n    v_passage RECORD;\r\n\r\n    v_question jsonb;\r\n    v_question_id bigint;\r\n    v_existing_question RECORD;\r\n\r\n    v_question_type_code text;\r\n    v_question_type_id bigint;\r\n    v_distinct_types text[];\r\n\r\n    v_option jsonb;\r\n    v_answer text;\r\n\r\n    v_correct_count int;\r\n\r\n    v_payload_question_ids bigint[] := '{}';\r\n\r\nBEGIN\r\n\r\n    PERFORM rpc_assert_min_role('teacher');\r\n\r\n    -- =========================\r\n    -- 1. BASIC VALIDATION\r\n    -- =========================\r\n    IF p_questions IS NULL OR jsonb_typeof(p_questions) <> 'array' THEN\r\n        RAISE EXCEPTION 'INVALID_QUESTIONS_PAYLOAD';\r\n    END IF;\r\n\r\n    IF jsonb_array_length(p_questions) = 0 THEN\r\n        RAISE EXCEPTION 'QUESTIONS_EMPTY';\r\n    END IF;\r\n\r\n    -- =========================\r\n    -- 2. HOMOGENEOUS TYPE CHECK\r\n    -- =========================\r\n    SELECT array_agg(DISTINCT q->>'question_type_code')\r\n    INTO v_distinct_types\r\n    FROM jsonb_array_elements(p_questions) q;\r\n\r\n    IF array_length(v_distinct_types, 1) != 1 THEN\r\n        RAISE EXCEPTION 'MIXED_QUESTION_TYPES_NOT_ALLOWED';\r\n    END IF;\r\n\r\n    v_question_type_code := v_distinct_types[1];\r\n\r\n    -- =========================\r\n    -- 3. TYPE VALIDATION\r\n    -- =========================\r\n    IF v_question_type_code NOT IN (\r\n        'passage_mcq', 'passage_text', 'audio_mcq', 'audio_text'\r\n    ) THEN\r\n        RAISE EXCEPTION 'INVALID_QUESTION_TYPE_FOR_PASSAGE';\r\n    END IF;\r\n\r\n    IF v_question_type_code LIKE 'audio_%' AND p_audio_url IS NULL THEN\r\n        RAISE EXCEPTION 'AUDIO_URL_REQUIRED';\r\n    END IF;\r\n\r\n    -- =========================\r\n    -- 4. RESOLVE TYPE ID\r\n    -- =========================\r\n    SELECT id INTO v_question_type_id\r\n    FROM question_types\r\n    WHERE code = v_question_type_code;\r\n\r\n    IF v_question_type_id IS NULL THEN\r\n        RAISE EXCEPTION 'INVALID_QUESTION_TYPE_CODE';\r\n    END IF;\r\n\r\n    -- =========================\r\n    -- 5. STRICT VALIDATION (NO WRITE)\r\n    -- =========================\r\n    FOR v_question IN SELECT * FROM jsonb_array_elements(p_questions)\r\n    LOOP\r\n\r\n        IF v_question->>'content' IS NULL THEN\r\n            RAISE EXCEPTION 'QUESTION_CONTENT_REQUIRED';\r\n        END IF;\r\n\r\n        -- MCQ\r\n        IF v_question_type_code IN ('passage_mcq', 'audio_mcq') THEN\r\n\r\n            IF jsonb_array_length(v_question->'options') < 2 THEN\r\n                RAISE EXCEPTION 'MCQ_MIN_2_OPTIONS';\r\n            END IF;\r\n\r\n            SELECT COUNT(*)\r\n            INTO v_correct_count\r\n            FROM jsonb_array_elements(v_question->'options') opt\r\n            WHERE (opt->>'is_correct')::boolean = true;\r\n\r\n            IF v_correct_count != 1 THEN\r\n                RAISE EXCEPTION 'MCQ_ONE_CORRECT_REQUIRED';\r\n            END IF;\r\n\r\n        END IF;\r\n\r\n        -- TEXT\r\n        IF v_question_type_code IN ('passage_text', 'audio_text') THEN\r\n\r\n            IF jsonb_array_length(v_question->'text_answers') = 0 THEN\r\n                RAISE EXCEPTION 'TEXT_ANSWER_REQUIRED';\r\n            END IF;\r\n\r\n        END IF;\r\n\r\n    END LOOP;\r\n\r\n    -- =========================\r\n    -- 6. LOCK PASSAGE\r\n    -- =========================\r\n    SELECT *\r\n    INTO v_passage\r\n    FROM passages\r\n    WHERE id = p_passage_id\r\n    FOR UPDATE;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'PASSAGE_NOT_FOUND';\r\n    END IF;\r\n\r\n    -- =========================\r\n    -- 7. UPDATE PASSAGE\r\n    -- =========================\r\n    UPDATE passages\r\n    SET\r\n        content = p_content,\r\n        audio_url = p_audio_url,\r\n        updated_at = now()\r\n    WHERE id = p_passage_id;\r\n\r\n    -- =========================\r\n    -- 8. UPSERT QUESTIONS\r\n    -- =========================\r\n    FOR v_question IN SELECT * FROM jsonb_array_elements(p_questions)\r\n    LOOP\r\n\r\n        v_question_id := NULLIF(v_question->>'id','')::bigint;\r\n\r\n        -- ===== UPDATE =====\r\n        IF v_question_id IS NOT NULL THEN\r\n\r\n            SELECT *\r\n            INTO v_existing_question\r\n            FROM questions\r\n            WHERE id = v_question_id\r\n              AND passage_id = p_passage_id\r\n              AND deleted_at IS NULL\r\n            FOR UPDATE;\r\n\r\n            IF NOT FOUND THEN\r\n                RAISE EXCEPTION 'QUESTION_NOT_FOUND %', v_question_id;\r\n            END IF;\r\n\r\n            UPDATE questions\r\n            SET\r\n                content = v_question->>'content',\r\n                explanation = v_question->>'explanation',\r\n                difficulty = (v_question->>'difficulty')::smallint,\r\n                updated_at = now()\r\n            WHERE id = v_question_id;\r\n\r\n        ELSE\r\n        -- ===== INSERT =====\r\n            INSERT INTO questions (\r\n                passage_id,\r\n                question_type_id,\r\n                content,\r\n                explanation,\r\n                difficulty,\r\n                created_by\r\n            )\r\n            VALUES (\r\n                p_passage_id,\r\n                v_question_type_id,\r\n                v_question->>'content',\r\n                v_question->>'explanation',\r\n                (v_question->>'difficulty')::smallint,\r\n                auth.uid()\r\n            )\r\n            RETURNING id INTO v_question_id;\r\n\r\n        END IF;\r\n\r\n        -- =========================\r\n        -- OPTIONS (MCQ)\r\n        -- =========================\r\n        IF v_question_type_code IN ('passage_mcq', 'audio_mcq') THEN\r\n\r\n            DELETE FROM options WHERE question_id = v_question_id;\r\n\r\n            FOR v_option IN\r\n                SELECT * FROM jsonb_array_elements(v_question->'options')\r\n            LOOP\r\n                INSERT INTO options (\r\n                    question_id,\r\n                    content,\r\n                    is_correct\r\n                )\r\n                VALUES (\r\n                    v_question_id,\r\n                    v_option->>'content',\r\n                    (v_option->>'is_correct')::boolean\r\n                );\r\n            END LOOP;\r\n\r\n        END IF;\r\n\r\n        -- =========================\r\n        -- TEXT ANSWERS\r\n        -- =========================\r\n        IF v_question_type_code IN ('passage_text', 'audio_text') THEN\r\n\r\n            DELETE FROM question_text_answers\r\n            WHERE question_id = v_question_id;\r\n\r\n            FOR v_answer IN\r\n                SELECT value FROM jsonb_array_elements_text(v_question->'text_answers')\r\n            LOOP\r\n                INSERT INTO question_text_answers (\r\n                    question_id,\r\n                    answer_text\r\n                )\r\n                VALUES (\r\n                    v_question_id,\r\n                    v_answer\r\n                );\r\n            END LOOP;\r\n\r\n        END IF;\r\n\r\n        -- =========================\r\n        -- TAGS\r\n        -- =========================\r\n        DELETE FROM question_tags WHERE question_id = v_question_id;\r\n\r\n        IF v_question ? 'tag_ids' THEN\r\n            INSERT INTO question_tags (question_id, tag_id)\r\n            SELECT v_question_id, value::bigint\r\n            FROM jsonb_array_elements_text(v_question->'tag_ids');\r\n        END IF;\r\n\r\n        v_payload_question_ids :=\r\n            array_append(v_payload_question_ids, v_question_id);\r\n\r\n    END LOOP;\r\n\r\n    -- =========================\r\n    -- 9. SOFT DELETE REMOVED QUESTIONS\r\n    -- =========================\r\n    UPDATE questions\r\n    SET deleted_at = now()\r\n    WHERE passage_id = p_passage_id\r\n      AND deleted_at IS NULL\r\n      AND id <> ALL(v_payload_question_ids);\r\n\r\n    RETURN p_passage_id;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_update_question",
    "identity_arguments": "p_question_id bigint, p_question_text text, p_explanation text, p_difficulty smallint, p_category_id bigint, p_grade_level integer, p_is_active boolean, p_last_updated_at timestamp with time zone, p_options jsonb, p_text_answers jsonb, p_tag_ids bigint[]",
    "arguments": "p_question_id bigint, p_question_text text, p_explanation text, p_difficulty smallint, p_category_id bigint, p_grade_level integer, p_is_active boolean, p_last_updated_at timestamp with time zone, p_options jsonb DEFAULT NULL::jsonb, p_text_answers jsonb DEFAULT NULL::jsonb, p_tag_ids bigint[] DEFAULT NULL::bigint[]",
    "return_type": "bigint",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": false,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_update_question(p_question_id bigint, p_question_text text, p_explanation text, p_difficulty smallint, p_category_id bigint, p_grade_level integer, p_is_active boolean, p_last_updated_at timestamp with time zone, p_options jsonb DEFAULT NULL::jsonb, p_text_answers jsonb DEFAULT NULL::jsonb, p_tag_ids bigint[] DEFAULT NULL::bigint[])\n RETURNS bigint\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_updated_id bigint;\r\nBEGIN\r\n  UPDATE questions\r\n  SET\r\n    question_text = p_question_text,\r\n    explanation = p_explanation,\r\n    difficulty = p_difficulty,\r\n    category_id = p_category_id,\r\n    grade_level = p_grade_level,\r\n    is_active = p_is_active,\r\n    updated_at = now()\r\n  WHERE id = p_question_id\r\n    AND deleted_at IS NULL  -- 🔴 GUARD\r\n    AND updated_at = p_last_updated_at -- optimistic lock\r\n  RETURNING id INTO v_updated_id;\r\n\r\n  IF v_updated_id IS NULL THEN\r\n    RAISE EXCEPTION 'QUESTION_NOT_FOUND_OR_DELETED_OR_CONFLICT';\r\n  END IF;\r\n\r\n  RETURN v_updated_id;\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_update_user_name",
    "identity_arguments": "p_name text",
    "arguments": "p_name text",
    "return_type": "TABLE(user_id uuid, name text, updated_at timestamp with time zone)",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_update_user_name(p_name text)\n RETURNS TABLE(user_id uuid, name text, updated_at timestamp with time zone)\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_user_id uuid;\r\n    v_name text;\r\n    v_current_name text;\r\n    v_is_active boolean;\r\n\r\nBEGIN\r\n\r\n    ----------------------------------------\r\n    -- 1️⃣ Identify caller\r\n    ----------------------------------------\r\n\r\n    v_user_id := auth.uid();\r\n\r\n    IF v_user_id IS NULL THEN\r\n        RAISE EXCEPTION 'UNAUTHENTICATED';\r\n    END IF;\r\n\r\n    ----------------------------------------\r\n    -- 2️⃣ Normalize name\r\n    ----------------------------------------\r\n\r\n    v_name := trim(p_name);\r\n\r\n    IF v_name IS NULL OR length(v_name) = 0 THEN\r\n        RAISE EXCEPTION 'INVALID_NAME';\r\n    END IF;\r\n\r\n    IF length(v_name) > 100 THEN\r\n        RAISE EXCEPTION 'NAME_TOO_LONG';\r\n    END IF;\r\n\r\n    ----------------------------------------\r\n    -- 3️⃣ Lock profile\r\n    ----------------------------------------\r\n\r\n    SELECT name, is_active\r\n    INTO v_current_name, v_is_active\r\n    FROM profiles\r\n    WHERE id = v_user_id\r\n    FOR UPDATE;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'PROFILE_NOT_FOUND';\r\n    END IF;\r\n\r\n    IF v_is_active = FALSE THEN\r\n        RAISE EXCEPTION 'USER_INACTIVE';\r\n    END IF;\r\n\r\n    ----------------------------------------\r\n    -- 4️⃣ Skip if unchanged\r\n    ----------------------------------------\r\n\r\n    IF v_current_name = v_name THEN\r\n        RETURN QUERY\r\n        SELECT id, name, updated_at\r\n        FROM profiles\r\n        WHERE id = v_user_id;\r\n\r\n        RETURN;\r\n    END IF;\r\n\r\n    ----------------------------------------\r\n    -- 5️⃣ Update name\r\n    ----------------------------------------\r\n\r\n    UPDATE profiles\r\n    SET\r\n        name = v_name,\r\n        updated_at = now()\r\n    WHERE id = v_user_id;\r\n\r\n    ----------------------------------------\r\n    -- 6️⃣ Return result\r\n    ----------------------------------------\r\n\r\n    RETURN QUERY\r\n    SELECT id, name, updated_at\r\n    FROM profiles\r\n    WHERE id = v_user_id;\r\n\r\nEND;\r\n$function$\n"
  },
  {
    "schema_name": "public",
    "function_name": "rpc_update_user_role",
    "identity_arguments": "p_user_id uuid, p_new_role text",
    "arguments": "p_user_id uuid, p_new_role text",
    "return_type": "TABLE(user_id uuid, role text, updated_at timestamp with time zone)",
    "language": "plpgsql",
    "provolatile": "v",
    "is_security_definer": true,
    "full_definition": "CREATE OR REPLACE FUNCTION public.rpc_update_user_role(p_user_id uuid, p_new_role text)\n RETURNS TABLE(user_id uuid, role text, updated_at timestamp with time zone)\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'public'\nAS $function$\r\n\r\nDECLARE\r\n    v_caller_id uuid;\r\n    v_caller_role text;\r\n\r\n    v_current_role text;\r\n    v_is_active boolean;\r\n\r\nBEGIN\r\n\t\r\n\tperform rpc_assert_min_role('admin');\r\n\r\n    ------------------------------------------------\r\n    -- 1️⃣ Validate input\r\n    ------------------------------------------------\r\n\r\n    IF p_user_id IS NULL THEN\r\n        RAISE EXCEPTION 'INVALID_USER_ID';\r\n    END IF;\r\n\r\n    IF p_new_role IS NULL THEN\r\n        RAISE EXCEPTION 'INVALID_ROLE';\r\n    END IF;\r\n\r\n    p_new_role := lower(p_new_role);\r\n\r\n    ------------------------------------------------\r\n    -- 2️⃣ Identify caller\r\n    ------------------------------------------------\r\n\r\n    v_caller_id := auth.uid();\r\n\r\n    IF v_caller_id IS NULL THEN\r\n        RAISE EXCEPTION 'UNAUTHENTICATED';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 3️⃣ Get caller role\r\n    ------------------------------------------------\r\n\r\n    SELECT role\r\n    INTO v_caller_role\r\n    FROM profiles\r\n    WHERE id = v_caller_id;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'PROFILE_NOT_FOUND';\r\n    END IF;\r\n\r\n    IF v_caller_role <> 'admin' THEN\r\n        RAISE EXCEPTION 'PERMISSION_DENIED';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 4️⃣ Lock target profile\r\n    ------------------------------------------------\r\n\r\n    SELECT role, is_active\r\n    INTO v_current_role, v_is_active\r\n    FROM profiles\r\n    WHERE id = p_user_id\r\n    FOR UPDATE;\r\n\r\n    IF NOT FOUND THEN\r\n        RAISE EXCEPTION 'USER_NOT_FOUND';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 5️⃣ Validate user state\r\n    ------------------------------------------------\r\n\r\n    IF v_is_active = FALSE THEN\r\n        RAISE EXCEPTION 'USER_INACTIVE';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 6️⃣ Validate role\r\n    ------------------------------------------------\r\n\r\n    IF p_new_role NOT IN ('student','teacher','admin') THEN\r\n        RAISE EXCEPTION 'INVALID_ROLE';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 7️⃣ Protect admin accounts\r\n    ------------------------------------------------\r\n\r\n    IF v_current_role = 'admin' THEN\r\n        RAISE EXCEPTION 'CANNOT_MODIFY_ADMIN';\r\n    END IF;\r\n\r\n    IF p_new_role = 'admin' THEN\r\n        RAISE EXCEPTION 'CANNOT_ASSIGN_ADMIN';\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 8️⃣ Skip update if role unchanged\r\n    ------------------------------------------------\r\n\r\n    IF v_current_role = p_new_role THEN\r\n        RETURN QUERY\r\n        SELECT id, role, updated_at\r\n        FROM profiles\r\n        WHERE id = p_user_id;\r\n\r\n        RETURN;\r\n    END IF;\r\n\r\n    ------------------------------------------------\r\n    -- 9️⃣ Update role\r\n    ------------------------------------------------\r\n\r\n    UPDATE profiles\r\n    SET\r\n        role = p_new_role,\r\n        updated_at = now()\r\n    WHERE id = p_user_id;\r\n\r\n    ------------------------------------------------\r\n    -- 🔟 Return result\r\n    ------------------------------------------------\r\n\r\n    RETURN QUERY\r\n    SELECT id, role, updated_at\r\n    FROM profiles\r\n    WHERE id = p_user_id;\r\n\r\nEND;\r\n$function$\n"
  }
]